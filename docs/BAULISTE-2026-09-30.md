# Maloja Plana — die eine Bau-Liste bis 30.09.2026

*Kanonisch seit 15.09.2026 abends HIER im Repo (vorher `stebler-studios/BAULISTE-maloja-bis-2026-09-30.md`, dort steht jetzt nur noch ein Zeiger). Angelegt 15.09.2026, 17:00, auf den Auftrag «ja mach das bitte» von Stebler Studios. Zusammengezogen aus den vier
bisherigen Listen, jede Zeile gegen Code, git oder Live-Seite geprüft, nicht abgeschrieben:*

| Quelle | Stand | Was davon hier landet |
|---|---|---|
| `docs/TODO.md` (531 Zeilen) | 24.06., Nachträge bis 19.07. | die noch offenen 🟠/🟡-Punkte; alles ✅/⏭️ ist weg |
| `docs/roadmap/BACKLOG_MASTER.md` (160 Items) | 17.05., **gesperrt** | nur gelesen, nichts geändert. 150 davon sind Backend/Governance-Runtime, bewusst zurückgestellt (`master-roadmap.md`) |
| `docs/IDEEN.md` (146 Zeilen) | 08.07. | die zwei «jetzt holbaren» Ideen; der Rest ist Vision und bleibt dort |
| Predeploy-Backlog A–J (Gedächtnis, 19.07.) | Juli | die offenen Meinungs-Entscheide und die Crosslink-Prüfung |

Dazu `BUGS.md` (0 offen), `RELEASE_CRITERIA.md` v0.6 (alle Muss-Punkte erfüllt), `FEATURES.md`
(alles bis #128 `verified-live`), Issue #123 (Wartung Q3, offen seit 01.09.) und `SESSION_START.md`
vom 15.09. (`main` = `0cc8e80`, PR #129 offen, Deploy 15:52, Branch-Wald leer).

**Diese Datei liegt im Studio-Repo, nicht im Maloja-Repo,** weil dort am 15.09. eine zweite
Sitzung arbeitet (PR #129). Sobald die Liste gestrichen ist, geht sie als `docs/BAULISTE-2026-09-30.md`
per PR ins Repo und `docs/TODO.md` bekommt oben einen Zeiger hierher.

**Regel:** Stebler Studios streicht, Claude Code baut. Jeder Punkt hat Quelle, Grösse (S = Stunden, M = ein Tag,
L = mehrere Tage) und wer die Hand hat. Kein Push, kein Merge, kein Deploy ohne ihr Ja. Jede
Sitzung startet aus `maloja-frontend/` mit `git fetch && git status` und der Frage, ob eine zweite
Sitzung läuft.

---

## 0 · Was «fertig gebaut bis 30.09.» heisst — Vorschlag

Die Release-Kriterien v0.6 sind alle erfüllt, die App ist seit 10.07. live. «Fertig» kann also
nicht «alle Ideen gebaut» heissen (das sind 160 + 40 + die Vision). Vorschlag für die Ziellinie:

1. **Nichts liegt halb.** Was gebaut, aber nicht aktiv ist (wageClaim, Tresor), ist entweder
   eingeschaltet oder trägt einen Zettel «bewusst geparkt, weil …».
2. **Keine offenen 🔴 oder P1** aus den Reviews vom Juli.
3. **Der Bug-Zyklus ist geschlossen:** Melde-Kanal funktioniert, Weg Mail → `BUGS.md` → Test →
   Fix steht in einem Satz, Wartung Q3 ist erledigt.
4. **Die Doku sagt die Wahrheit:** eine Bau-Liste, ein Release-Tag für den Stand seit Juli,
   Changelog nachgeführt.
5. **Ein Voll-Review über die ganze App** (Stufe L, alle neun Prüfer) hat null Blocker.
6. **Die offenen Meinungs-Entscheide sind entschieden** (ja, nein oder «Oktober»).

Alles andere ist Vision und wartet auf Oktober. Wenn Stebler Studios eine andere Ziellinie will, ist das
der erste Strich in dieser Liste.

---

## 1 · MUSS bis 30.09. (12 Punkte)

| # | Punkt | Quelle | Grösse | Hand | Stand |
|---|---|---|---|---|---|
| M1 | **Wartung Q3 (Issue #123):** `npm audit`, Vite/React/Vitest-Versionen, Minor-Updates, 5 Stichproben-Links aus `direktLinks.js`, Build + Chunk-Grössen dokumentieren, Issue schliessen | Issue #123, `docs/operations/maintenance-prompt.md` | S | Claude (PR), Stebler Studios merged | ✅ Q3-Protokoll gemergt (#131); Issue #123 bleibt offen bis zur Analytics-Zeile aus dem Infomaniak-Panel (Hand von Stebler Studios) |
| M2 | **Sitemap `lastmod` live + IndexNow-Ping:** PR #129 mergen, kleiner Deploy, `bash scripts/indexnow-ping.sh`, per `curl` belegen | `SESSION_START.md` 15.09. | S | Stebler Studios (Merge + Deploy) | ✅ erledigt (#129, #130): Sitemap live mit `lastmod` 2026-09-14 (`curl` 17.09.); IndexNow-Ping laut Briefing HTTP 200, nicht selbst gemessen — siehe §6 |
| M3 | **Release-Tag + Changelog:** `v0.1.27-beta` für den Stand seit 19.07. (#107 … #128), `package.json` anheben, `[Unreleased]` → Version | `CHANGELOG.md`, `FEATURES.md` («kein Release-Tag gesetzt») | S | Claude (PR) | ✅ erledigt: Tags `v0.1.27-beta` (#139), `v0.1.28-beta`, `v0.1.29-beta` (= `2fcf409`, live seit 17.09. 01:46) |
| M4 | **Bug-Zyklus schliessen, Teil Kanal:** der Feedback-Knopf ist `mailto:info@malojaplana.ch` (`main.jsx:918`). `docs/TODO.md` §G führt «info@malojaplana.ch Postfach einrichten» als **offen** → prüfen, ob das Postfach existiert und ankommt (Testmail) | `main.jsx:918`, TODO §G | S | **Stebler Studios** (Postfach), Claude (Testmail-Weg beschreiben) | ✅ **erledigt:** das Postfach `info@malojaplana.ch` funktioniert laut Stebler Studios seit Langem (Auskunft 17.09.2026); keine Testmail nötig |
| M5 | **Bug-Zyklus schliessen, Teil Weg:** ein Absatz in `BUGS.md`: «Mail an info@ → nachstellen → Zeile hier → roter Test → Fix → Zeile streichen». Plus: Kunden-Meldungen ans Studio sind ein Studio-Thema, nicht Maloja | `BUGS.md`, TODO-portfolio §C | S | Claude (PR) | ✅ gemergt (#132): `BUGS.md` «Wie ein Bug hierher kommt»; der Eingang selbst hängt an M4 |
| M6 | **Backup-Restore-Härtung:** Validierung in `ZipExport.jsx:147-158` als echte Barriere statt Warnung, Grössenlimit vor `FileReader`, Anzahl-Caps, `merkliste` in `validateBackupPayload` | TODO §G2 (reproduce-first 19.07.: echte Lücke) | M | Claude (PR) | ✅ gemergt (#133), live seit 0.1.27-beta |
| M7 | **wageClaim: einschalten oder Zettel.** Offene 🔴 aus Runde 8: Netto/Brutto ungeprüft · Brief auch bei Befund `ok` · `unpaidWage` behauptet 1 Monatslohn für unbekannten Zeitraum · GE/TI/JU `verify:false` nie geprüft. Dazu zwei Datenfragen (13. Monat im LSE-Median ≈ 8 % Aufschlag · GE-Ausnahmen/Sätze) | `FEATURES.md`, Gedächtnis wageClaim-Reentry | **L** (fixen) oder **S** (Zettel wie `src/crypto/README.md`) | **Stebler Studios entscheidet** (→ E1), Claude baut | ✅ Zettel gemergt (#132), Funktion bleibt aus; **E1 auf Oktober geparkt** (§12) |
| M8 | **Rohe Unicode-Glyphen als Icons, erste Hälfte:** `◰ ● ◇ ↧ □ ✕` in ~102 Stellen / 10 Views. Zuerst Sozialhilfe (`SozialhilfeView.jsx:79/125/148/163`), Schulden, und die Barometer-Legenden `▬ ● ▏` als echte Legenden-Symbole | TODO §G3 P1 («a11y → nie unter P1»), Stebler Studios 19.07. | M | Claude (PR), Polygrafin + a11y-Prüfer | ✅ erste Hälfte gemergt (#135): Sozialhilfe, Schulden, Barometer-Legenden |
| M9 | **Voll-Review Stufe L** über die ganze App: alle neun Prüfer nach `bibliothek/sops/review-umfang.md`, Judge-Prinzip (ich plane und prüfe, günstige Agenten führen aus). Befunde → diese Liste, 🔴 werden abgearbeitet | TODO-portfolio §C, `review-umfang.md` | L, token-teuer, **einmalig** | Claude, Stebler Studios gibt das Budget frei | ✅ gelaufen 15.09. (9/9, `docs/audits/voll-review-L-2026-09-15.md`); 🔴 abgearbeitet, der letzte (M13 IPV) über E9 (#167, #168) |
| M10 | **Deploy-Tag mit allem oben:** PRs mergen, `/maloja-predeploy`, `bash deploy.sh`, drei `curl`, `FEATURES.md` auf `verified-live`, `SESSION_START.md` nachziehen. Erwartung aus #128: Backup ~150 Dateien statt 5696 | `DEV_WORKFLOW.md` | S | Stebler Studios (Deploy), Claude (Vorbereitung + Belege) | ✅ Deploys 15.09. 22:14 (0.1.27-beta) und 17.09. 01:46 (0.1.29-beta), je per `curl` belegt (§7, §13). Gemergt, nicht live: #186–#188, #191 |
| M11 | **Listen zusammenführen:** diese Datei ins Repo, `docs/TODO.md` bekommt oben den Zeiger, `IDEEN.md` bleibt die Vision. `BACKLOG_MASTER.md` bleibt gesperrt, bis Schritt 2 (gemeinsam durchgehen, ~1 h) gelaufen ist → E3 | 🛑 Sperre aus `entscheidungs-matrix.md` | S | Claude (PR), Stebler Studios (die Stunde) | ✅ gemergt (#132): diese Datei gilt; `BACKLOG_MASTER.md` bleibt gesperrt bis E3 (Oktober) |
| M12 | **Kurz-DSFA (Datenschutz-Folgenabschätzung):** eine Seite unter `docs/legal/`, weil die App öffentlich ist und Gesundheits-/Sozial-/Finanzdaten verarbeitet, wenn auch nur lokal. Rechts-Prüfer gegenlesen | TODO §G2 🟠 («vor Produktivstart») | S, docs-only | Claude (PR), Stebler Studios liest | ✅ Entwurf gemergt (#132), Rechts-Doku-Widersprüche behoben (#138); juristische Prüfung offen (K48) |

*Spalte «Stand» nachgeführt 17.09.2026 gegen `git`, `gh` und `curl`. Der Verlauf steht in §6, §7 und §13. Von den zwölf MUSS ist nur **M4** offen (Hand von Stebler Studios); dazu kommen M15 und O8 im Panel.* *Nachtrag 17.09.2026: M4 erledigt → **alle zwölf MUSS erledigt**; offen bleibt der Panel-Durchgang (M15, O8, O20, HSTS, K72).*

## 2 · KANN bis 30.09. (klein, sicher, wenn Zeit bleibt)

| # | Punkt | Quelle | Grösse |
|---|---|---|---|
| K1 | Anklickbarer `[[BFS]]`-Link am Armutsgrenzen-Befund, 5 Sprachen (`renderSource`) | TODO §B (Link-Checker-Fund) | S |
| K2 | «Vor dem Wechsel prüfen»-Schritt auch in `ZusatzWechsel` (in `KVGWechsel` seit #101 live) | IDEEN §13 | S |
| K3 | Export-Vorschau «Das verlässt dein Gerät: …» vor Export/Dossier/Brief | IDEEN §13 | M |
| K4 | Kurzes helles Aufblitzen im Dunkelmodus: `data-theme` schon in `index.html` setzen | FEATURES (#96 Nit) | S |
| K5 | Rohe Glyphen, zweite Hälfte (BudgetImport, Merkliste, KKScanner, Mietzins, ZipExport, Stipendien, FinanzUebersicht, KVGLeistungen) | TODO §G3 | M |
| K6 | a11y-Kür: Fokusring auf Sprach-Buttons prüfen · `soft`-auf-Karten (braucht Token-Entscheid, 87 Stellen) · Fokusring-Farbe | TODO §H, Runde 11.07. | S–M |
| K7 | Demo-Modus am Einstieg («ohne Code ausprobieren») — **nur wenn das BetaGate live noch aktiv ist** (→ E6) | TODO §H, Stebler Studios «sehr gerne» | M |
| K8 | Mammografie-Geografie 3-II/3 (LU/NW/OW/UR ohne Programm): prüfen, ob mit «Mammografie-Geo» (PRs #47–53) schon gebaut; sonst S | TODO §Braindump-Fäden | S |
| K9 | Per-Modul-Freigabe-Register (Rechner · Fachperson · Rechtsgrundlage · Version · Kanton) als Tabelle in `docs/` | TODO §G2 🟡 | S |
| K10 | Crosslink-Prüfung «Kanton am Anfang → überall übernommen; IPV-Rechner übernimmt Eingaben nicht» — nachstellen, dann Bug oder erledigt | Predeploy-Backlog §C («die Magie von Maloja») | S (prüfen) |
| K11 | `docs/TODO.md` §A: 129 tote `rm.js`-Keys — bewusst gelassen (AST-Diff zu gross); bleibt gelassen, nur vermerken | TODO §A | — |

## 3 · Entscheide, die nur Stebler Studios fällen kann (je ein Satz genügt)

| # | Frage | Meine Empfehlung | Hängt dran |
|---|---|---|---|
| E1 | **wageClaim:** die vier 🔴 fixen und einschalten, oder bis nach dem Datenentscheid (13. Monat, GE-Sätze) mit Zettel parken? | Zettel. Die zwei Datenfragen sind nicht in zwei Wochen sauber zu belegen, und ein halb richtiger Lohnbrief ist Haftung | M7 |
| E2 | **Tresor 2b-UI:** bleibt vertagt bis zur Logins-Phase? | Ja, wie am 14.07. entschieden. Nichts anfassen | — |
| E3 | **BACKLOG_MASTER-Sperre:** eine Stunde gemeinsam durchgehen, dann archivieren? | Ja, diese Woche. 150 der 160 Items sind Backend/Runtime, die `master-roadmap.md` schon als zurückgestellt führt | M11 |
| E4 | **Meinungs-Entscheide vom 19.07.:** Dynamic Pricing/Paywall · Affiliate-Links · B-Corp/MyClimate · KK-Schulden übernehmen ja/nein · Readdle-Empfehlung · PWA vs. Native | Alle sechs auf «Oktober», ausser PWA: dabei bleiben (Material rät klar dazu) | Ziellinie Punkt 6 |
| E5 | **Kapitel-Architektur nach Lebensdomäne** (Gesundheit eigener Bereich; Arbeit + Vorsorge + Steuer zusammen) | Oktober, eigene Design-Session. Zu gross für zwei Wochen | IDEEN §6 |
| E6 | **Ist das BetaGate live noch aktiv?** (Code `maloja2026` laut `master-roadmap.md`) | Wenn ja: K7 wird MUSS, weil «Wert zeigen bevor man fragt» beide Audits als höchste Priorität nannten | K7 |
| E7 | **Kurz-DSFA** jetzt (docs-only) oder Oktober? | Jetzt, eine Seite. Die App ist produktiv | M12 |
| E8 | **Nächstes grosses Thema nach dem 30.09.:** Stipendien-Verzeichnis oder Rega/Notfall-QR? | Erst nach dem Voll-Review entscheiden, mit dessen Befunden | Oktober |

## 4 · Bewusst NICHT bis 30.09. (Vision, bleibt in `IDEEN.md` und im Gedächtnis)

Skeuomorphismus-Welt und Metaphern · Lebensbaum/Obstgarten · Schnellchecks als Instrumente
(Design-Phase) · House of Life · Generatoren (Vorsorgeauftrag, Testament, Petition, Lebenslauf,
Mietvertrag) · Asylwesen + weitere Sprachen · RM-Gegenlese (263 Keys, Muttersprachler) · Backend,
Konten, Sync, SwissID · Governance-Runtime P1-003 bis P2-031 · Haushalt teilen/Berechtigung ·
Gamification · Markenschutz IGE (Geld, Stebler Studios) · QR-Flyer und Pilot-Outreach (Marketing kommt
nach «fertig») · Business-Mail-Entscheid (hängt an M4).

## 5 · Tagesblöcke — Vorschlag für 11 Arbeitstage

| Tag | Maloja | Stebler Studios (parallel, Hand von Stebler Studios) |
|---|---|---|
| Di 16.09. | Stebler Studios streicht diese Liste (30 min). Claude: M1 Wartung Q3 + M3 Release-Tag als PRs vorbereiten | HTTPS-Zertifikat im Infomaniak-Panel · Formspree-Konto |
| Mi 17.09. | M6 Backup-Härtung (PR) · M5 Bug-Weg in `BUGS.md` · M4 Testmail an info@ | Geschäftsadresse + E-Mail festlegen |
| Do 18.09. | M8 Glyphen Sozialhilfe/Schulden/Barometer (PR) | Kundenfreigaben für die Referenz-Karten anfragen (drei Kunden) |
| Fr 19.09. | M7 wageClaim gemäss E1 · M12 Kurz-DSFA (PR) | Impressum/Datenschutz zur juristischen Prüfung geben |
| Mo 22.09. | **Deploy-Tag 1:** M2 + M1/M3/M5/M6/M8/M12 mergen, `/maloja-predeploy`, Deploy, `curl`, Tag setzen (M10) | — |
| Di–Do 23.–25.09. | M9 Voll-Review Stufe L, Befunde abarbeiten | Studio: `noindex` raus, `deploy.sh`, Search Console, Google Business Profil |
| Fr 26.09. | Reste aus dem Review · M11 Listen ins Repo · E3-Stunde | — |
| Mo 29.09. | **Deploy-Tag 2:** Review-Fixes live, `verified-live`, `SESSION_START.md` | — |
| Di 30.09. | Puffer. Bilanz gegen Abschnitt 0 | Bilanz gegen `LAUNCH-1-2-3.md` |

Ehrlich dazu: M9 ist der grösste und teuerste Block. Fällt er weg, ist die Liste in einer Woche
durch, aber Punkt 5 der Ziellinie bleibt offen.

---

## 6 · Messungen vom 15.09. abends (17:20–17:40) — nichts gebaut, nur belegt

Stebler Studios fragte «kannst du heute schon gewisse Dinge machen?». Gemacht wurde alles, was nur misst.
Jede Zeile hat eine Gegenprobe; Details im Sitzungsverlauf.

| Punkt | Befund | Folge für die Liste |
|---|---|---|
| **M2** | **Erledigt, bevor die Liste stand:** PR #129 und #130 gemergt, `main`=`4b922ac`, Sitemap live mit 5× `lastmod` 2026-09-14, IndexNow-Ping laut Briefing HTTP 200 | M2 streichen. Im Tagesblock Mo 22.09. entfällt «M2» |
| **E6** | **Das BetaGate ist live aktiv.** `main.jsx:1400` hüllt die App in `BetaGate`; im Live-Bundle `index-2d6da893.js` stehen der Code-Hash `bc0d786f…` und der Schlüssel `or5_beta_access` je 1×, erfundener Hash 0×. Der Klartext-Code steht nirgends (nur SHA-256), also ist `maloja2026` aus `master-roadmap.md` nicht belegbar, nur der Hash | **K7 (Demo-Modus) wird MUSS**, wie in E6 vorgesehen. E6 ist damit entschieden, kein Strich nötig |
| **M4** | `dig MX malojaplana.ch` → `mta-gw.infomaniak.ch` (erfundene Domain: kein MX). Die Domain nimmt Mail an; **ob das Postfach `info@` existiert, sagt DNS nicht.** Das entscheidet erst die Testmail | Bleibt Hand von Stebler Studios (Postfach im Infomaniak-Panel + Testmail). Ohne Postfach zeigt der Feedback-Knopf ins Leere |
| **K8** | **Schon gebaut und richtig.** `MAMMO_KANTONE_OHNE_PROGRAMM = ZH ZG SZ UR OW NW GL SH` (Stand 07.2026). Die Quelle swisscancerscreening.ch (heute abgerufen) führt **Luzern unter den Brustkrebs-Programmen**, LU gehört also nicht in die Ohne-Liste; UR/OW/NW stehen drin. Der TODO-Satz «LU/NW/OW/UR ohne Programm» war überholt | K8 streichen (erledigt seit PR #47/#55) |
| **M1** | `npm audit`: **0 Lücken in Produktions-Abhängigkeiten**, 12 nur in Dev (vite 4 / vitest / tar / postcss / xmldom …). `npm outdated`: 13 Pakete, davon Majors react 19, vite 8, vitest 5, eslint 10. Issue #123 seit 01.09. offen | Bleibt S, aber die Majors gehören **nicht** in Q3 (Bruch-Risiko), nur Minor/Patch |
| **M3** | Letzter Tag `v0.1.26-beta` = `package.json`; **46 Commits** seither auf `main` | Bleibt wie beschrieben |

**Was sich daraus ändert:** 12 MUSS → 11 (M2 weg) + K7 rückt hoch → wieder 12. KANN 11 → 10 (K8
weg). Entscheide 8 → 7 (E6 gemessen). *Nachgetragen 15.09. 17:45, vor den Strichen von Stebler Studios.*

---

## 7 · Stand am 15.09. spätabends — was der Abend gebracht hat

Auf den Auftrag «m1 bis m12 versuchen» hin gebaut. Jede Zeile hat ihren PR oder ihre Datei als
Beleg. Gemergt hat Stebler Studios; nichts ist ohne ihr Ja live gegangen.

| # | Stand | Beleg |
|---|---|---|
| M1 | Q3-Protokoll gemergt. Issue #123 bleibt offen, bis die Analytics-Zeile aus dem Infomaniak-Panel abgelesen ist (Hand von Stebler Studios) | #131, Kommentar im Issue |
| M2 | erledigt (siehe §6) | — |
| M3 | **erledigt:** Version `0.1.27-beta` + Changelog (#139), Tag `v0.1.27-beta` = `f368fbb` nach dem Deploy gesetzt und auf origin | #139, `git ls-remote --tags` |
| M4 | offen, Hand von Stebler Studios: Postfach `info@` im Panel prüfen, Testmail senden | §6 |
| M5 | gemergt | #132, `BUGS.md` «Wie ein Bug hierher kommt» |
| M6 | gemergt | #133, 22 neue Tests, Bundle 63.7 kB |
| M7 | Zettel gemergt, **Entscheid E1 steht aus** | #132, `TASK_befund-brief-lohn.md` |
| M8 | gemergt: Sozialhilfe und Schulden ohne rohe Glyphen, Barometer-Legenden als SVG | #135 |
| M9 | **gelaufen:** 9 von 9 Prüfern, 11 🔴 · 30 ⚠️ · 22 💡. 10 der 11 🔴 behoben und gemergt (#134, #136, #138); offen nur M13 | `docs/audits/voll-review-L-2026-09-15.md` |
| M10 | **erledigt: Deploy 15.09. 22:14 aus `f368fbb`** (Hand von Stebler Studios). `curl` 22:20: `index-4a2ca1ae.js` 200, `0.1.27-beta` im Bundle, altes Bundle 404, Gegenprobe 404; Backup 161/115 Dateien. `FEATURES.md` auf `verified-live`, `SESSION_START.md` nachgezogen | Stand-PR 15.09. spätabends |
| M11 | gemergt | #132, diese Datei |
| M12 | Entwurf gemergt; die sechs Doku-Widersprüche daraus behebt der Rechts-Doku-PR | #132, `dsfa-kurzfassung.md` §7 |

**Aus dem Voll-Review neu dazu**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| M13 | **IPV-Beträge:** `CANTONAL_IPV` ist mustergeneriert (Familie = 2× Einzel, Kind = ½, Grenzen auf 3000 gerundet, erfundener linearer Abbau), zeigt aber «Berechtigt» und einen CHF-Betrag und fliesst ins Budget (`budgetSync.js`). Entweder Betrag zurückhalten und nur Orientierung zeigen, oder je Kanton belegen | S (zurückhalten) · L (26 Kantone belegen) | Entscheid E9 |
| M14 | **erledigt** 15.09. 22:14: `git pull`, Deploy, `curl`, Tag — siehe M10 | S | — |
| M15 | **Infomaniak-Panel: `Permissions-Policy` auf `geolocation=(self)` stellen.** Live liefert der Server `geolocation=()` und schaltet den Standort damit ganz ab; die Notfall-Vorlesekarte ruft ihn aber auf (`NotfallVorlesekarte.jsx`). Bekannt seit Runde 7 (`FEATURES.md`), am 15.09. erneut gemessen | S | Stebler Studios (Panel) |
| K12 | Elterntarif DBG Art. 36 Abs. 2bis für Alleinerziehende (heute Grundtarif + Kinderabzug) | M | Claude, mit ESTV-Quelle |
| K13 | Kantons-/Gemeindesteuer-Faktor: auf ein Einkommensband begrenzen oder als «grob» kennzeichnen | S | Entscheid |
| K14 | Datenquellen 2026 nachziehen: Tardoc-Taxpunktwerte · ALV-Werte 2026 · KLV Art. 38a (Generika-Selbstbehalt) · 13. Rente bei Ehepaaren (Art. 34ter AHVG) · LIK-Basis *(korrigiert 15.09. spätabends: hier stand «KVV» und «Art. 34bis»; 34bis ist der Zuschlag der Übergangsgeneration, gefunden beim Bau von #148)* | M | Claude, gegen amtliche Quellen |
| K15 | Lockfile erneuern, danach `npm ci` in beiden Workflows (der Versuch in #134 scheiterte: das Lockfile führt die Plattform-Pakete von esbuild 0.28 nicht) | S | Claude |
| K16 | Kleinkram aus dem Review: Polygrafin-px-Labels im Dashboard · tote Exporte (`kkScanner`, `csvImport`, `schuldenCalc`, `cvGenerator`) · `sw.js` `openWindow` auf die eigene Origin begrenzen · Kalender-Knopfgruppe `role="group"` · Sozialhilfe «Nächste Schritte» h4 → h3 | S–M | Claude |
| K17 | Das Export-Werkzeug heisst in Hilfetexten und FAQ «ZIP-Export» und verspricht eine «verschlüsselte ZIP-Datei» (`faq.a1/a5/a7`, `securityTip2`, 5 Sprachen); geschrieben werden aber Einzeldateien (JSON, CSV, verschlüsselt `.maloja`). Die Datenschutz-Texte sind in #134 korrigiert, die Benennung in Hilfe und FAQ noch nicht | S | Claude (Wortwahl mit Stebler Studios) |

**Neue Entscheide**

| # | Frage | Empfehlung |
|---|---|---|
| E9 | IPV-Beträge zurückhalten oder je Kanton belegen? | Bis 30.09. zurückhalten, im Oktober belegen. Eine mustergenerierte Zahl mit «Berechtigt» daneben ist dieselbe Haftungsfrage wie beim Lohnbrief |
| E10 | Backup-Passphrase: Mindestlänge von 4 auf 12 anheben? | Ja für neue Backups; alte 4er-Backups müssen weiter entschlüsselbar bleiben |
| E11 | `src/assets/icons-full/` (18 Dateien, unreferenziert) und die Münz-Assets archivieren? `architektur.html` (gitignored) einchecken? | Archivieren mit `_WAS-IST-DAS.md`; die Architektur-Karte einchecken, weil das Cockpit sie über `/karte/maloja` liest |
| E12 | Impressum: vollständige Post- oder c/o-Adresse (UWG Art. 3 Abs. 1 lit. s)? | Ja. Dieselbe Frage hat die Studio-Website |

**Ein Fund in eigener Sache:** Diese Liste und das Q3-Protokoll nannten die Inhaberin 23 Mal beim
Vornamen, und §5 nannte drei Kunden namentlich. Das Repo ist öffentlich, und der PII-Scan in
`deploy.sh` hätte beim nächsten Deploy angehalten. In diesem PR korrigiert; der Scan läuft danach
leer (Beleg im PR).

---

## 8 · Fremd-Audit (ChatGPT) gegen den Code gelegt — 15.09. spätabends

**Was hereinkam:** Stebler Studios hat mit ChatGPT rund zehn Audit-Runden gemacht. Hier liegen im
Wortlaut vor: **Teil 8** (Architektur), **Teil 9** (Master-Roadmap Beta → 0.2 → 0.5 → 0.9 → 1.0),
**Teil 10** (Backlog MP-001 bis MP-100 mit Sprint-Plan). Von **Teil 1–7** (Dashboard/Navigation ·
Lebensmappe/Datenmodell · Steuern/IPV/Sozialhilfe/Finanzen · Security/Backup · Dokumente ·
Lebensereignisse · Design/Accessibility) kennen wir nur die Überschriften und das, was davon in
Teil 9/10 wieder auftaucht. Siehe 8.5.

**Warum geprüft statt übernommen:** Die Fremd-KI schreibt selbst, der Code sei «in meiner aktuellen
Runtime nicht mehr gemountet». Ihre Grundlage war also ältere Doku. Jede Zeile unten ist gegen
`main` = `525300f` gelegt.

**Regel:** Diese Datei bleibt die eine Bau-Liste. Die 100 Tickets werden **nicht** als Liste
übernommen, sonst gäbe es wieder fünf Listen, wie vor dem 15.09. Hier stehen nur die Punkte, die
im Code Bestand haben. An §1–§7 und am Ziel 30.09. ändert dieser Abschnitt nichts.

### 8.1 Schon gebaut — das Fremd-Audit nahm «fehlt» an

| Fremd-Ticket | Beleg im Code |
|---|---|
| MP-011 Schema-Version | `_version`, aktuell 4 (`src/utils/dataMigration.js:12`) |
| MP-012 Migrations-Rahmen | `migrations[]` v0 → v4, der Reihe nach, reine Funktionen (`docs/governance/migration-policy.md`) |
| MP-013 Snapshot vor der Migration | `or5_data_premigration`; ohne Snapshot keine Migration (`snapshot_failed`) |
| MP-014 Rollback | bei Fehler zurück auf den Snapshot (`migration_failed`) |
| «neuere Version nicht überschreiben» | `migrateData` lässt `_version > 4` unberührt; `validateData` entfernt keine unbekannten Schlüssel (Kommentar: «may be from future versions») |
| MP-093 Import prüfen | `validateBackupPayload` mit Listen-Grenzen, seit #133 (M6) eine echte Sperre |
| MP-088 Export-Kopf | Backup-Format trägt `version: '5.1'` und `created` (`src/utils/backupCrypto.js:54`) |
| Code-Splitting | 66× `lazy()` in `main.jsx` |
| i18n-Parität | Test über **fünf** Sprachen inkl. Rumantsch (`src/i18n/__tests__/i18n.test.js`), nicht vier |
| «KKScanner: CDN deps» | keine externe URL mehr; QRCode.js 1.0.0 liegt in `src/vendor/` (`VENDOR.md`) |
| MP-091 Header | CSP self-only in `index.html`; nosniff, Referrer- und Permissions-Policy in `public/.htaccess`. Offen nur M15 |
| `showEmergencyHub`-Flag | nicht mehr im Code (0 Treffer) |
| ARCHITECTURE, ADRs, Prinzipien, DoD «neu anlegen» | gibt es: `docs/architecture/decision-records.md` (ab ADR-001), ADR-009 bis ADR-011, `zielarchitektur-2026.md`, `definition-of-done.md`, `docs/product/principles.md`, `docs/roadmap/master-roadmap.md`. Zusammenführen ja, neu schreiben nein |

### 8.2 Stimmt — echte Kandidaten für Oktober

| # | Punkt | Beleg | Grösse | Einschätzung |
|---|---|---|---|---|
| O1 | **Eine Routen-Tabelle** (MP-001) | `VALID_VIEWS` steht getrennt in `src/utils/hashRouter.js:20`, die Auswahl der Ansicht als `view === '…'`-Kette in `main.jsx` (ab Z. 1208), die Navigation ein drittes Mal. Der Notfall-Dossier-Bug war genau dieses Muster | S–M | Bester Einstieg in die Architektur: klein, testbar, sofort nützlich. Eine Tabelle `{ id, component, nav }`, die alle drei lesen, plus ein Test «jede Nav-ID ist eine gültige Route» |
| O2 | **CSV-Export gegen Formeln schützen** (MP-089) | `generateCSVBackup` in `src/zipExport.js` setzt Anführungszeichen, fängt aber Zellen mit `=` `+` `-` `@` am Anfang nicht ab. Wer die CSV in einer Tabellenkalkulation öffnet, bekommt solche Zellen als Formel ausgeführt | S | Die einzige Sicherheitslücke aus der Fremd-Liste, die sich im Code bestätigt. Die Daten sind zwar die eigenen, ein wiederhergestelltes Backup kann aber von woanders stammen. Passt zu K3/K17 (Export), die gerade in einer anderen Arbeitskopie gebaut werden → **nach deren Merge**, nicht dazwischen |
| O3 | **Ergebnis-Arten als festes Feld** (MP-034/036): Berechnung · Schätzung · Vorprüfung · Orientierung, dazu «noch n Angaben nötig» | Heute löst jeder Rechner das selbst (Orientierungs-Texte in mindestens zehn Dateien); `src/data/orientationRegistry.js` gilt nur für Kapitel-Felder. M13 ist der akute Fall | M | Nach E9 als gemeinsames Feld einführen, dann Rechner für Rechner umstellen |
| O4 | **Quellen im Code** (MP-031/032): ein Format mit Stelle, Jahr, `geprüftAm` | Quellen stehen in `docs/sources/source-inventory.md` (Mai) und verstreut in einzelnen Dateien unter `src/data/` | M | Das Format zuerst für die Dateien, die K14 ohnehin anfasst. Kein Grossumbau |
| O5 | **Ein Schreibweg für den Speicher** (MP-015/016) | 22 Dateien schreiben direkt in `localStorage`, über 20 `or5_*`-Schlüssel. `src/utils/storage.js` ist als Kern da | M–L | Nicht um des Umbaus willen. Zuerst die Regel «neue Stellen nur über `storage.js`» plus ein Zähler in der CI, der nicht steigen darf |
| O6 | **Grosse Dateien zerlegen** (MP-007/008) | `ChapterView.jsx` 2308 Zeilen (die grösste, das Fremd-Audit nennt sie nicht), `Dashboard.jsx` 1736, `main.jsx` 1414; `allData` 22× in `ChapterView.jsx` | L | Erst nach O1 und O3, sonst wird doppelt umgebaut. Hier stimme ich dem Fremd-Audit (Teil 8, §59) zu |
| O7 | **«Nächster Schritt»** (MP-076–080): deterministisch, mit Satz «warum jetzt?» | gibt es nicht | L | Die stärkste Produktidee der Fremd-Liste. Braucht zuerst O3 und ein Vorgangs-Modell. Eigene Design-Sitzung, wie E5 |

### 8.3 Überlegungen — wo ich dem Fremd-Audit folge und wo nicht

- **Reihenfolge.** Das Fremd-Audit beginnt bei Schema und Migration; dieses Fundament steht seit
  Mai. Der Engpass ist nicht die Architektur, sondern die **fachliche Wahrheit**: M13, K13, K14, E1.
  Ein erfundener CHF-Betrag neben «Berechtigt» schadet mehr als 1414 Zeilen `main.jsx`.
- **100 Tickets.** Die Liste mischt Gebautes, Vision und Vorschläge. Als Arbeitsliste wäre sie die
  fünfte Liste; die Lehre vom 15.09. («vier Listen, keine gilt») spricht dagegen.
- **IndexedDB als Hauptspeicher (MP-017).** ADR-009 lässt IndexedDB und localStorage nebeneinander
  zu, ein Umzug widerspricht ihm also nicht. Aber Nutzerdaten umzuziehen ist das Riskanteste, was
  eine lokale App tun kann, und der Nutzen ist heute nicht belegt. → Nur mit eigenem ADR-Nachtrag
  und gemessenem Grund.
- **TypeScript.** Neue Fach-Dateien könnten `.ts` sein, das kostet wenig. Einen Umbau des
  Bestands sehe ich nicht vor 1.0.
- **Zod oder eine andere Schema-Bibliothek.** Nein. `CLAUDE.md` sagt «dependency bloat vermeiden»,
  und `dataValidation.js` leistet das schon.
- **Golden-Tests aus amtlichen Rechenbeispielen (MP-048/097).** Ja, und billig. Der AHV-Fall vom
  15.09. (der Test war falsch, der Code richtig) zeigt: die Erwartung muss aus der Quelle kommen,
  nicht von einem Agenten.
- **Externer Security-Review und Fachreview Sozialhilfe/Steuern vor 1.0 (MP-099, Teil 9 «0.9»).**
  Ja. Das ersetzen auch die neun eigenen Prüfagenten nicht. Kostet Geld → E15.
- **Die zehn 1.0-Versprechen aus Teil 9** sind ein brauchbarer Entwurf für `RELEASE_CRITERIA.md`
  v1.0 (heute v0.6) → E16.
- **Positionierung** («Ordnung in dein Leben in der Schweiz … privat auf deinem Gerät») ist eine
  Marken- und Copy-Frage, keine Code-Frage. Gehört in eine Marken-Sitzung, nicht in dieses Repo.

### 8.4 Neue Entscheide

| # | Frage | Empfehlung |
|---|---|---|
| E13 | Fremd-Audit: O1–O7 als Oktober-Kandidaten übernehmen, den Rest der 100 Tickets verwerfen? | Ja |
| E14 | O2 (CSV-Formelschutz) noch vor dem 30.09., nach dem Merge von K3/K17? | Ja, S |
| E15 | Externer Security-Review und Fachreview vor 1.0 budgetieren? | Ja, Betrag im Oktober festlegen |
| E16 | Die zehn 1.0-Versprechen als Entwurf für `RELEASE_CRITERIA.md` v1.0? | Ja, als Entwurf zum Streichen |

### 8.5 Was noch fehlt

**Nichts mehr.** Alle Teile liegen im Wortlaut vor und sind geprüft: Teil 8–10 in 8.1–8.4, die
Übersicht und Teil 1–6 in 8.6, Teil 7 in 8.7. Teil 8–10 kamen ein zweites Mal mit; der Wortlaut
ist inhaltlich derselbe. Die Texte selbst gehören **nicht in dieses öffentliche Repo** (sie nennen
Personen und den Beta-Zugang), sondern falls gewünscht in den Studio-Ordner unter `_zum-lesen/`.

### 8.6 Nachgereicht: Übersicht und Teil 1–6 — gegen `ecb1f9b` gelegt

Die Fremd-KI hat diese Teile mit einer ZIP von **v0.1.26-beta** geprüft, also vor #131–#141.
Deshalb ist ein Teil davon schon erledigt. Jede Zeile unten ist am heutigen Code oder live belegt.

**Schon erledigt oder schon so gebaut**

| Fremd-Punkt | Beleg |
|---|---|
| Versionen und Release-Tag aufräumen | `0.1.27-beta`, Tag gesetzt (M3) |
| SKOS-Vermögensfreibetrag seit 1.1.2026 CHF 6'000 statt 4'000 (Teil 3) | Der Rechner rechnet schon so: `vermoegensfreibetragSKOS` 6'000 / 12'000 nach SKOS-RL D.3.1, Test `sozialhilfeRechner.test.js:140`. Quelle am 15.09. auf skos.ch nachgelesen: «Freibetrag von neu 6000 statt 4000 Franken». Veraltet ist nur der Dateikopf → D1 |
| Security-Header müssen vom Server kommen (Teil 4) | live gemessen 15.09.: `strict-transport-security: max-age=16000000`, `x-content-type-options: nosniff`, `referrer-policy`, `x-frame-options: SAMEORIGIN`, `permissions-policy` (dort offen: M15). Nur die CSP fehlt als Header → O8 |
| «Zero Knowledge» nicht öffentlich versprechen | In den App-Texten (`de.js`, `en.js`) kein Treffer. Das Wort steht nur in `src/crypto/README.md` |
| Backup-Krypto DEK/KEK/WebAuthn-PRF gelobt (Teil 4) | Das ist `src/crypto/vault.js`, laut README **bewusst dormant, im Produkt nicht verdrahtet**. Das Lob gilt einem Baustein, den niemand erreicht. Live verschlüsselt `backupCrypto.js` mit Passphrase (dazu E10) |
| Brief nicht mit Maloja als Absender | `briefGenerator.js` nennt Maloja nirgends |
| «Sandbox» für Nutzer umbenennen | heisst im UI schon «Probier-Modus» |
| Nächste fehlende Angabe gibt es schon | ja: `nextField` in `Dashboard.jsx:805` |
| Backup-Status aufs Dashboard | eine Zeile «zuletzt gesichert … / noch nie» ist da (`Dashboard.jsx:748`, `:855`); ohne Alter in Tagen |
| Prämienregion aus der PLZ ableiten | BAG-Datensatz je Gemeinde mit Region (`praemienRegionen.js`), genutzt in `PraemienOrientierung.jsx` |
| Referenzalter nicht fest auf 65 | #136 (Art. 55ter AHVV, Platzhalter), `TASK_ahv21-referenzalter-frauen.md` |
| JSON-Export versionieren | `version: '5.0'` und `created` im Export; die Daten tragen `_version` |
| ZIP mit Manifest | `MANIFEST.txt` wird erzeugt; dass es gar kein ZIP ist, klärt K17 |
| DOCX für Briefe | Maloja hat keinen DOCX-Export; die Empfehlungen dazu sind gegenstandslos |
| Progressive Disclosure, Lebensbaum als Spiegel, Suche, Mobile-Anker | bestätigt, keine Aufgabe |

**Stimmt — neu dazu**

| # | Punkt | Beleg | Grösse | Einschätzung |
|---|---|---|---|---|
| O8 | **CSP als echter HTTP-Header** | Live kommt keine `content-security-policy` vom Server, nur das Meta-Tag in `index.html`. Dort stand `frame-ancestors 'none'`, das wirkt im Meta-Tag laut CSP-Spezifikation nicht (Konsolenfehler, Lighthouse); am 17.09.2026 aus dem Meta-Tag entfernt. Clickjacking deckt heute `X-Frame-Options: SAMEORIGIN` | S | Derselbe Handgriff im Infomaniak-Panel wie M15, mit der Policy aus dem Meta-Tag **plus** `frame-ancestors 'none'` → E19 |
| O9 | **Die Vollständigkeit zählt Leeres als offen** | `Dashboard.jsx:645–647`: `filled / fields.length`, gefüllt heisst «truthy». Einen Zustand «trifft nicht zu» gibt es nicht (nur einen Sonderfall Kinder, `ChapterView.jsx:1803`). Die Grundordnung (MVO in `config/constants.js`) verlangt `employer`, `phone`, `email`: wer pensioniert, in Ausbildung oder ohne Stelle ist, kommt nie auf 100 % | S (MVO) · M (Zustand) | Der stärkste Befund aus Teil 2, weil er die Würde-Haltung berührt: Maloja soll niemanden als «unvollständig» zeigen, der alles Zutreffende ausgefüllt hat. Erster Schritt klein: `employer`, `phone`, `email` von Pflicht auf «empfohlen» → E17. Danach der Zustand «trifft nicht zu» |
| O10 | **Die Hauptaktion ist fest die Finanzübersicht** | `primary: true` bei `highlightFinanz` (`Dashboard.jsx:1218`). Der «nächste Schritt» ist nur die nächste MVO-Angabe | S (Hervorhebung) | Gehört zu O7. Bis dahin reicht, die Hervorhebung auf die nächste Angabe zu legen. Design-Entscheid |
| O11 | **Kein «Alle Daten auf diesem Gerät löschen»** | Nicht gefunden: `storage.clear()` in `utils/storage.js` hat keinen Aufrufer, in `de.js` steht kein Text dafür. Löschen geht heute nur über die Browser-Einstellungen | S–M | Für eine App, die mit «alles bleibt auf deinem Gerät» wirbt, gehört ein ruhiger, auffindbarer Löschweg mit Bestätigung und dem Angebot «vorher sichern» dazu. Rechts-Prüfer (nDSG) → E18 |
| O12 | **Die Abläufe merken sich nichts** | Keine der Ablauf-Dateien (`AblaufSchale`, `UmzugAblauf`, `StelleVerloren`, `Todesfall`, `Trennung`, `Gepaeck`) liest oder schreibt Speicher. Es gibt kein Startdatum, keine Rolle beim Todesfall und keine Art beim Umzug (innerhalb der Gemeinde, anderer Kanton, Ausland). Zeitbezug gibt es nur über die Erinnerung (`addReminder` in `AblaufSchale.jsx`) | L | Das ist der Kern von Teil 5 und 6 («Vorgänge») und dasselbe Thema wie O7. Kommt nach O3. Eigene Design-Sitzung |
| O13 | **Dokumente ohne Lebenszyklus** (Teil 5) | Tresor-Einträge kennen schon `expiryDate` und `status`; es fehlen die Unterscheidung Original/Scan/Kopie und ein Stand zum Zeitpunkt der Erstellung bei erzeugten Briefen | M–L | Oktober oder später. Erst mit O12 sinnvoll |
| O14 | **Rumantsch wird wie eine fertige Sprache angeboten** | Die Sprachwahl zeigt «Rumantsch» ohne Zusatz (`Onboarding.jsx:54`), `rm.js` ist laut Zeile 2 provisorisch | S | Ein Zusatz wie «provisorisch» ist ehrlicher, bis die Gegenlese (§4) gelaufen ist. Copy-Entscheid |
| D1 | **SKOS-Dateikopf nachführen** | `sozialhilfeRechner.js:1` sagt «Stand 1.1.2025, für 2026 unverändert», `SKOS_DATA_VERSION = '2025-01'`; die Rechnung selbst ist auf 2026 (D.3.1) | S | Doku-Drift, kein Rechenfehler |

**Überlegungen zu Übersicht und Teil 1–6**

- **Teil 3 ist der wertvollste.** Seine Muster für die Anzeige (IPV als «Vorprüfung» mit Genauigkeit,
  Sozialhilfe als «Orientierung» mit Spanne statt Einzelbetrag) sind eine gute Vorlage für O3 und
  für die Umsetzung von E9.
- **Fremd-Audits haben ein Datum.** Geprüft wurde v0.1.26; mehrere Punkte hat der Abend des
  15.09. schon erledigt. Die Zahlen darin (Zeilen, Tests) gelten für damals.
- **Gelobt wurde teilweise Code, der nicht läuft.** Das Krypto-Lob in Teil 4 gilt dem dormanten
  `vault.js`. Fremd-Urteile deshalb immer gegen den Pfad lesen, den Nutzer wirklich erreichen.
- **Nicht übernehmen:** eine «Metaphern-Grenze» und Umbenennungen (Design-Entscheid, gehört in die
  E5-Sitzung); eine öffentliche `/security`-Seite und SEO-Themenseiten (Oktober, `docs/SEO_GEO.md`;
  Umbauten an den Pfaden der Seite nur mit eigenem Entscheid).
- **Objekte statt Einzelfelder** (`employment[]`, `insurancePolicies[]`, Teil 2): fachlich richtig,
  aber jede solche Änderung ist eine Migration v4 → v5. Der Rahmen dafür steht (8.1); der Anlass
  sollte ein konkreter Fall sein, nicht Aufräumen.

**Neue Entscheide**

| # | Frage | Empfehlung |
|---|---|---|
| E17 | O9: `employer`, `phone`, `email` aus der Pflicht-Grundordnung nehmen (nur noch «empfohlen»)? | Ja. Klein, und niemand wird mehr als unvollständig gezeigt, nur weil er keinen Arbeitgeber hat |
| E18 | O11: Löschweg «Alle Daten auf diesem Gerät löschen» noch vor dem 30.09.? | Ja, mit Rechts-Prüfer |
| E19 | O8: CSP-Header im Panel zusammen mit M15 setzen? | Ja, gleicher Handgriff, gleiche Policy wie im Meta-Tag |

### 8.7 Nachgereicht: Teil 7 (Design, Mobile, Accessibility) — gegen `ecb1f9b` gelegt

Teil 7 schreibt selbst, dass es sich auf «die bereits untersuchte App-Struktur» stützt, also nicht
mehr auf den Code. Viele Punkte sind deshalb Grundsätze. Belegbar im Code sind die folgenden.

**Schon so gebaut**

| Fremd-Punkt | Beleg |
|---|---|
| Feste Skalen für Abstand, Radius, Schrift | gibt es: `config/tokens.js` und `tokens.css` (`--mp-space-*` 4/8/16/24/32, `--mp-radius-*` 6/10/16/24/voll, `--mp-text-*` 13/15/16/19/23). Offen ist die Nutzung → O15 |
| Bottom-Navigation mit höchstens fünf Zielen | fünf: Übersicht · Kalender · + · Ansprüche · Menü (`main.jsx:346–356`) |
| Autosave statt Speichern-Knopf | `AutoSaveStatus.jsx` |
| Offline-Zustand sichtbar | `isOffline` mit den Ereignissen `online`/`offline` (`main.jsx:505`, `:593`) |
| Installieren nicht aufdrängen | `beforeinstallprompt` wird abgefangen (`main.jsx:627`); wann der Hinweis erscheint, ist nicht geprüft |
| Einstellungen gruppieren | zwei Gruppen, Darstellung und Barrierefreiheit (`settingsGroups.js`) |
| Keine Gamification | keine Streaks, Punkte oder Ranglisten in `de.js` |
| `<button>` statt `div role="button"` | nur zwei Ausnahmen (`FinanzUebersicht.jsx`, `ChapterView.jsx`) |
| Sichtbarer Fokus | globales `:focus-visible` für Buttons und Links (`tokens.css:147`) |
| Dark Mode nicht überpriorisieren | ist schon gebaut; offen nur K4 |
| Die vier Metaphern behalten, keine fünfte | deckt sich mit §4 und E5 |

**Stimmt — neu dazu**

| # | Punkt | Beleg | Grösse | Einschätzung |
|---|---|---|---|---|
| O15 | **Die Tokens werden oft umgangen** | 954 Zahlenwerte für `margin`/`padding` direkt im JSX, häufig ausserhalb der Skala: 6 (90×), 10 (83×), 2 (71×), 20 (69×), 14 (32×). `borderRadius` mit 2, 3, 4, 5, 8, 9, 12, 20 neben den Token-Radien. 40× `fontSize` unter 14 als fester Wert | M, schrittweise | Kein Grossumbau. Regel «neue Stellen nur mit Tokens» plus ein CI-Zähler, der nicht steigen darf (wie O5). Das Dashboard kommt dran, wenn O6 kommt → E33 |
| O16 | **Farbnamen statt Rollen** | Die Paletten in `config/constants.js` heissen `sage`, `sageDeep`, `rose` …; in der Palette für Farbenblinde ist `sage` blau (`#1565A3`, Z. 65). Die Schlüssel sind also schon Rollen, tragen aber Farbnamen. Die kleinste Schriftstufe `text.xs` ist 13 px | M | Umbenennen auf Rollen lohnt nur zusammen mit einem Kontrast-Durchgang über alle Farbpaare (a11y-Prüfer), sonst ist es Kosmetik. Gehört zu K6 → E33 |
| O17 | **Dialoge ohne Dialog-Verhalten** | Nur `Tour.jsx` hat `role="dialog"` und `aria-modal`. Die anderen Vollflächen-Überlagerungen (eine in `main.jsx`, das Menü in `MobileNav.jsx`, `GlossarBegriff.jsx`) haben keine Dialog-Rolle. Eine Fokusfalle oder Fokus-Rückgabe gibt es im ganzen Code nicht (gesucht: `focusTrap`, `trapFocus`, `previouslyFocused`, `returnFocus`, `lastFocus`). Escape wird in 6 Dateien behandelt | S–M | Der konkreteste Accessibility-Befund aus Teil 7: Wer mit Tastatur oder Screenreader arbeitet, kann hinter der Überlagerung landen. Ein kleiner gemeinsamer Hook (Fokus hinein, Tab bleibt drin, Escape schliesst, Fokus zurück) für alle vier → E32 |
| O18 | **`outline: none` an 13 Stellen** | Das globale `:focus-visible` deckt Buttons und Links. Ob die 13 Stellen (darunter Eingabefelder) einen Ersatz haben, ist nicht geprüft | S | Einzeln prüfen, zusammen mit O17 |
| O19 | **Die Suche findet nur Werkzeuge und Kapitel** | `SearchView.jsx` vergleicht mit den Navigations-Beschriftungen. Dokumente, Einträge der Lebensmappe und Fristen findet sie nicht | M | Oktober oder später, nach O12 |
| O20 | **Keine Cache-Angabe für gehashte Dateien** | PageSpeed Insights 16.09.2026 (Mobil 98): `/assets/*.js`, `/assets/*.css` und `/fonts/*.woff2` kommen mit Cache-TTL «None», 252 KiB. `curl -sI …/assets/index-2301b4b1.js` zeigt keinen `cache-control`. Über `public/.htaccess` geht es nicht: `deploy.sh` löscht die Datei (Infomaniak liefert sonst 503). Der Service Worker (`public/sw.js`, registriert in `main.jsx:574`) liefert `/assets/` ab dem zweiten Besuch schon aus dem eigenen Cache | S | Panel-Handgriff zusammen mit M15/O8: für `/assets/` und `/fonts/` `Cache-Control: public, max-age=31536000, immutable`, **nicht** für `index.html`, `sw.js`, `theme-init.js` (keine Hashes im Namen). Kann das Panel das nicht pfadweise, bleibt es beim Service Worker. Im selben Zug HSTS von 16 000 000 auf 31 536 000 anheben; `preload` erst nach eigenem Entscheid |

**Überlegungen zu Teil 7**

- **Eine dominante Fläche «Als Nächstes»** ist dieselbe Baustelle wie O10 und O7, keine eigene.
- **Bottom-Navigation auf Übersicht · Vorgänge · Mappe · Suchen · Mehr umstellen:** erst, wenn es
  Vorgänge gibt (O12). Heute wäre «Vorgänge» leer; Kalender und Ansprüche bleiben bis dahin.
- **Presets wie «Ruhiger» oder «Einfacher»:** sinnvoll, aber später. Zuerst O17 und O18, das sind
  die Grundlagen, die heute Menschen ausschliessen.
- **Card-System und Komponenten-Bibliothek:** richtig, aber nur mit O6 zusammen. Einzeln
  gebaut, würde es vor dem Umbau des Dashboards noch einmal umgebaut.

**Neue Entscheide** *(Auf dem Zweig vom 15.09. hiessen sie E20/E21. Weil dieser Abschnitt erst am 16.09. auf `main` kam und §9 die Nummern E20/E21 schon trug — und Stebler Studios am 16.09. auf §9-E20 geantwortet hat —, führen sie hier E32/E33.)*

| # | Frage | Empfehlung |
|---|---|---|
| E32 | O17: gemeinsamer Dialog-Hook für die vier Überlagerungen noch vor dem 30.09.? | Ja, mit a11y-Prüfer. Betrifft Tastatur- und Screenreader-Nutzende direkt |
| E33 | O15/O16: Regel «neue Stellen nur mit Tokens» plus CI-Zähler jetzt, Umbenennung auf Rollen erst mit dem Kontrast-Durchgang? | Ja zur Regel jetzt, Umbenennung im Oktober |

---

## 9 · K-Runde 15.09. spätabends — Stand und neue Fragen

Auf «kommen wir zu K?» hin gebaut: acht Arbeitskopien parallel, je ein PR mit grüner CI. Stebler
Studios hat alle gemergt (22:53–23:24, zuletzt #146 und der Doku-PR #151) und um **23:25 deployt**:
live `index-f4a79e77.js` aus `f482576`, alle 118 Build-Dateien live 200, altes Bundle 404
(Beleg in `SESSION_START.md`). Die Footer-Version zeigt noch `0.1.27-beta`.

| # | Stand | PR |
|---|---|---|
| K1, K2 | gemergt | #147 |
| K3, K17 | gemergt. Titel und Wortwahl zum Gegenlesen → E25 | #145 |
| K4, K15 | gemergt | #142 |
| K5, K16 | gemergt. Der Konflikt mit #145 in `ZipExport.jsx` wurde vorher aufgelöst (Klick öffnet die Vorschau, Knopf zeigt das Icon) | #146 |
| K6 | Fokusring nachgerechnet: hell und dunkel auf allen Flächen mindestens 4:1, nichts zu tun. Offen nur `soft` auf Karten (Token-Entscheid) | #146 |
| K7 | gemergt. Acht kleine Produktentscheide stehen im PR-Body | #150 |
| K8 | erledigt, siehe §6 | — |
| K9, K10, K11 | gemergt. K10 fand B-1 und B-2 (`BUGS.md`) | #149 |
| K12 | gemergt | #144 |
| K13 | offen, Entscheid | — |
| K14 | gemergt | #148 |

**Aus der Runde neu**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K18 | Dashboard-Mini-Beschriftungen (Baum 7.5/9 px, Berg 9 px, Status 10 px; `Dashboard.jsx:427/589/626/1092`). Mit den 13 px aus dem Review passen «Versicherungen» und die Status-Spalte nicht mehr | S–M | E20 + Sichtprüfung |
| K19 | Zeichen-Sprache app-weit (`ⓘ ✓ → ↗ ▾ ▸ ○ •`) als eigener Durchgang; #135 und #146 haben sie bewusst stehen lassen | M | Claude, nach E21 |
| K20 | Export-Vorschau auch vor Finanzübersicht drucken, Notfall-Zusammenfassung, IPV-Antrag (JSON), Budget-Bericht, Lebenslauf, Kalender `.ics` | S–M | Claude |
| K21 | «ZIP» in der Rechts- und Produkt-Doku: `docs/legal/datenschutzerklaerung-ndsg.md` (Z. 108, 119), `docs/legal/bearbeitungsverzeichnis-ndsg.md` (Z. 150), mehrere `docs/product/*` | S | Claude, mit der Rechts-Doku-Runde |
| K22 | Tardoc-Taxpunktwerte der übrigen 23 Kantone belegen, je Kanton aus der Verfügung. Zwei von drei geprüften wichen ab | M | Claude, gegen kantonale Quellen |
| K23 | DBG Art. 36 Abs. 3 fehlt in allen Tarifen: Abrunden auf 100 Franken und auf 5 Rappen, keine Steuer unter 25 Franken | S | Claude |
| K24 | **Zu prüfen:** Im Beispiel-Modus *innerhalb* der App wirken Dokument-Upload und -Löschen vermutlich auf die echten Dokumente (`handleAddDocument`, `handleDeleteDocument`). Fund beim Bau von #150 aus dem Code, noch nicht nachgestellt; die Demo von der Code-Wand ist nicht betroffen | S | Claude: erst nachstellen, dann `BUGS.md` |
| K25 | Demo leiser machen: im Live-Browser (15.09. spätabends) schreibt die Demo `IDB getAllKeys error` und `[backup] Failed: Demo: es wird nichts gespeichert` in die Konsole, und nach «Beispiel verlassen» bleibt die letzte Ansicht in der Adresse (`#/export`). Nichts davon ist sichtbar, der echte Speicher bleibt nachweislich leer | S | Claude |
| O2 | CSV-Formelschutz: die Bedingung «nach dem Merge von K3/K17» ist erfüllt | S | wartet auf E14 |

**Nachtrag 16.09., 12:30: deployt um 12:23 als 0.1.28-beta** (`index-2301b4b1.js`, Tag gesetzt, Beleg in `SESSION_START.md`); B-3 steht unter «Zuletzt behoben».

**Stand 16.09., 12:10 — zweite Runde gemergt, nicht deployt.** K20 (#155) · K21 (#153) · K22 (#157) ·
K23 (#154) · K24 und K25 (#156) sind gemergt, `main` = `8c1bd46`, 951 Tests grün. Offen aus dieser
Tabelle bleiben **K18** und **K19** (beide warten auf E20/E21) sowie **O2** (E14). Vier der Agenten
wurden nachts vom Nutzungslimit abgebrochen; ihre angefangene Arbeit lag unkommittiert in den
Arbeitskopien und wurde am Morgen weitergebaut. **K24 war ein echter Bug** und ist als B-3 in
`BUGS.md` eingetragen.

**Neu aus der zweiten Runde**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K26 | Fussnote `kvg.tpwNote` sagt nicht, dass die Taxpunktwerte 2026 fast überall **provisorisch** sind (KVG Art. 46 Abs. 4) und dass neun Kantone auf Stand 2025 stehen. 5 Sprachen | S | Claude, nach E28 |
| K27 | Taxpunktwerte je Datenblock datieren statt über die eine `KVG_DATA_VERSION` (heute `v2026-06`, gilt für den ganzen KVG-Datensatz); dazu `docs/legal/freigabe-register.md` Z. 47 («Stand 2025») nachziehen | S | Claude, nach E29 |
| K28 | Fünf Widersprüche Doku ↔ Code aus #153 auflösen, zuerst `security/backup-strategy.md` Z. 17: dort steht, die Sicherung sei immer verschlüsselt — im Code ist sie optional, Standard ist Klartext-JSON | S | Claude, nach E31 |
| K29 | Rätoromanisch: die `TODO(rm)`-Platzhalter aus #145, #147, #150 und #155 sammeln und gegenlesen lassen; dazu O14 (die Sprachwahl bietet Rumantsch ohne Hinweis auf den provisorischen Stand an) | M | Stebler Studios (Muttersprachler), dann Claude |
| K30 | Neun Kantone ohne Beleg: AG · BL · SO · AI · GL · SH · JU · NE · VS. Beleg nur über direkte Anfrage beim kantonalen Gesundheitsamt oder der Ärztegesellschaft. Zwei Spuren aus der Suche: **JU** — das Amtsblatt 2026 nennt für freie Praxen keinen Wert, die Zahl steht in einem nicht publizierten Anhang zur kantonalen Vereinbarung; publiziert ist nur der Spital-ambulante Wert 0.89. **NE** — ein Begrüssungsleitfaden der Société neuchâteloise de médecine nennt 0.91 für die Grundversicherung (Stand 27.11.2025, also vor Inkrafttreten, keine Behörde, Einkaufsgemeinschaften nicht unterschieden). **Bewusst nicht übernommen**, ein einzelner Beleg dieser Art trägt eine Tarifzahl nicht; er macht die Anfrage aber gezielt. **VS** — kein Wert 2026 belegbar. Die App zeigt 0.86, deren Herkunft unklar ist: behördlich belegt ist zuletzt der Staatsratsbeschluss vom 3.12.2018 mit 0.84 für freie Praxen, von den Versicherern ans Bundesverwaltungsgericht gezogen; laut Bulletin officiel vom 16.01.2025 ist das Verfahren für die freipraktizierenden Ärzte «toujours en cours». Der Tarif liegt vermutlich als Vereinbarung der Walliser Ärztegesellschaft mit den Einkaufsgemeinschaften vor, öffentlich nicht einsehbar. Ein kursierender Wert 0.94 ist **nicht** bestätigt | M | Stebler Studios (Anfragen), dann Claude |

**Neue Entscheide**

| # | Frage | Empfehlung |
|---|---|---|
| E20 | K18: kleinerer Beschriftungs-Token, Kurzlabels oder Silbentrennung? | Kurzlabels. Lesbare Grösse geht vor Platz; braucht eine Sichtprüfung |
| E21 | K19: wann der Durchgang über die Zeichen-Sprache? | Oktober, nach O3 |
| E22 | B-1: Zahlen aus dem Schnellcheck ins Profil schreiben oder nur an den IPV-Rechner übergeben? | Nur übergeben und im IPV-Rechner sichtbar sagen. Der Schnellcheck verspricht «zum Ausprobieren» |
| E23 | B-2: Woher nimmt der Steuerrechner den Kanton? | Aus `behoerden.cantoneOfTaxation`, vorbelegt mit `basis.canton`. Steuer- und Wohnkanton können verschieden sein |
| E24 | D-1: PLZ-Kanton und Profil-Kanton weichen ab — welcher gilt? | So lassen, wie gebaut, aber ein ruhiger Hinweis, wenn beide verschieden sind |
| E25 | Export-Vorschau: Titel «Das steht in der Datei» (gebaut) oder «Das verlässt dein Gerät»? | Gebaut lassen. Die Datei landet zuerst auf dem eigenen Gerät |
| E26 | LIK-Basis auf Dezember 2025 wechseln? | Oktober, mit dem nächsten BFS-Stand |
| E27 | CI auf Node 24 (npm 11) umstellen, damit das Lockfile nicht mit npm 10 erzeugt werden muss? | Ja, kleiner PR. Bis dahin gilt der Hinweis in `ci.yml` |
| E28 | Taxpunktwerte: die neun unbelegten Kantone weiter mit Zahl zeigen, oder dort nur eine Orientierung ohne Betrag? | Zahl zeigen, aber die Fussnote sagt «provisorisch» und nennt die neun Kantone mit Stand 2025 (K26). Anders als bei der Prämienverbilligung steht hier kein Anspruch im Raum, nur eine Tarifangabe |
| E29 | Datenstand je Block führen statt einer Version für den ganzen KVG-Datensatz? | Ja, klein (K27). Sonst datiert eine Zahl den ganzen Katalog mit |
| E30 | Rätoromanisch: die Platzhalter gegenlesen lassen, oder die Sprache bis dahin als provisorisch kennzeichnen? | Beides. Kennzeichnen sofort (O14), Gegenlese im Oktober (K29) |
| E31 | `security/backup-strategy.md`: Doku an den Code angleichen, oder die Voreinstellung auf verschlüsselt ändern? | Doku jetzt angleichen (K28). Die Voreinstellung gehört zu E10, wo auch die Mindestlänge der Passphrase steht |

---

## 10 · Stand 16.09.2026, 18:00 — Entscheide, Ergebnisse, Oktober-Paket

**Entschieden von Stebler Studios am 16.09.:**

| # | Entscheid | Umgesetzt |
|---|---|---|
| E9 | «Beides»: Beträge der Prämienverbilligung sofort zurückhalten **und** die 26 Kantone amtlich belegen; ein belegter Kanton bekommt seinen Betrag zurück | #167, #168 (Zurückhalten); #161 (Belege) |
| E14 | CSV-Formelschutz jetzt | #163 |
| E22 | B-1: Schnellcheck-Zahlen an den IPV-Rechner übergeben **und** ins Profil übernehmen können — Speichern nur auf ausdrücklichen Klick | #167 |
| E23 | B-2: Steuerrechner nutzt `cantoneOfTaxation` **und** fragt bei Abweichung, ob der Wohnkanton mitgeändert werden soll — nur auf ausdrücklichen Klick | #165 |
| E34 | Vermögensfreibetrag «je Kanton, gekennzeichnet» | #169, #170 (Belege #166) |
| E35 | Die Nachfragen bei Kantonen (K30, K32) **im Oktober**. Bis dahin sagt die App dort nichts Falsches: unbelegte Werte sind gekennzeichnet oder ohne Betrag | — |
| E13 · O2 | CSV-Formelschutz war O2 aus §8.2 → mit E14 erledigt | #163 |

**Am selben Abend (~17:40) in einer parallelen Sitzung entschieden** — hier festgehalten, damit
sie als Datei gelten; umgesetzt ist davon noch nichts:

| # | Entscheid |
|---|---|
| E10 | Mindestlänge 12 Zeichen für neue Sicherungen **und verschlüsselt als Voreinstellung**; alte 4er-Sicherungen bleiben lesbar. Beantwortet auch die Voreinstellungs-Frage aus E31 |
| E17 | `employer`, `phone`, `email` nur noch «empfohlen» **und** je Feld ein Zustand «trifft nicht zu» (M statt S) |
| E18 | Löschweg «Alle Daten löschen» noch vor dem 30.09., mit Rechts-Prüfer |
| §9-E20 · K18 | Kurzlabels **und** Silbentrennung, mit Sichtprüfung |
| E28 | Taxpunktwerte: Zahl zeigen, Fussnote «provisorisch» (K26) |
| E29 | ja, Datenstand je Block (K27) |
| E30 | beides: Rumantsch sofort als provisorisch kennzeichnen (O14), Gegenlese im Oktober (K29) |
| K13 | beides: Kantons-Steuerfaktor kennzeichnen **und** ausserhalb des geeichten Einkommensbands zurückhalten |
| E27 | ja, CI auf Node 24 in einem kleinen PR |
| E3 | Oktober |

**Rückgefragt, noch nicht entschieden:** E1 (wageClaim) und der Walliser Taxpunktwert — die
Antworten «1–3» passten nicht, weil sich die Optionen gegenseitig ausschliessen.

`main` = `77bf8b1`, 1072 Tests grün, Hauptbundle 64.66 kB von 65. **Nicht live** (Deploy 17:45 am
Backup-Tor gestoppt, siehe `SESSION_START.md`).

**Ergebnisse der Recherchen, die künftige Arbeit tragen**
- **Prämienverbilligung (#161):** 21 Kantone abbildbar, 5 teilweise (SZ, GL, ZG, SO, TG), 0 unbelegt. Die bisherigen App-Beträge lagen überall zu tief, die Grenzen in vielen Kantonen zu hoch, in NW, OW, ZH, GE, NE, SO zu tief; kein Kanton rechnet «Familie = 2× Einzel, Kind = ½». Widersprüche zwischen amtlichen Quellen in NE, VS, ZH, BS, AG, JU. Neun Kantone hat der Hauptagent nur über Unteragenten geprüft (BE, LU, UR, SZ, AI, SG, GR, SH, AR). Ab Herbst erscheinen die Werte 2027.
- **Vermögensfreibetrag (#166):** 19 Kantone kantonal belegt (davon mehrere nur als Verweis auf SKOS), FR und VD mit veralteter Quelle, BL, AI, OW, TI nur SKOS-Karte, SG nur indirekt. SKOS D.3.1 (6'000 / 12'000 / +3'000 / max. 15'000) über vier amtliche Quellen bestätigt; der Richtlinientext selbst war technisch nicht abrufbar.
- **Rechts-Doku (#164):** fünf rechtliche Wertungen gemeldet, nicht geändert — u. a. ob eine **unverschlüsselte Sicherung als Voreinstellung** mit DSG Art. 7 Abs. 3 (Datenschutz durch Voreinstellung) vereinbar ist (→ E10/E31), und ob das Restrisiko «gering» (Datenschutzerklärung) oder «mittel» (DSFA) ist. Neu belegt: die automatischen Schnappschüsse liegen unverschlüsselt, ohne Wiederherstellung in der Oberfläche.

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K31 | **Prämienverbilligung je Kanton einbauen** (E9, zweiter Teil): pro Kanton das amtliche Modell aus #161 rechnen, Tests mit amtlichen Rechenbeispielen, dann `beleg` setzen. Vorschlag: zuerst ZH, BE, VD, AG, SG, GE. Vor jedem Kanton den Stand an der Quelle neu prüfen (Werte 2027 ab Herbst). Die Formel «Familie 2×, Kind ½, linearer Abbau» gilt für keinen Kanton | L | Claude, Reihenfolge durch Stebler Studios |
| K32 | **Vermögensfreibetrag: Nachfragen** bei BL, AI, OW, TI und zur Lesart AG («Kinder zählen als Person»); dazu SH mit Kindern | S je Kanton | Stebler Studios, **Oktober** (E35) |
| K30 | Taxpunktwerte der neun Kantone ohne Beleg (siehe §9) | S je Kanton | Stebler Studios, **Oktober** (E35) |
| K33 | Steuerkanton auch in der Finanzübersicht (`FinanzUebersicht.jsx:141`) und im Steuererklärungs-Link (`OfficialLinkBox.jsx:14`) nutzen; heute noch der Wohnkanton | S | Claude, nach Zusage |
| K34 | Automatische Schnappschüsse: unverschlüsselt und ohne Wiederherstellung in der Oberfläche — Doku sagt es jetzt. Mit E10 («verschlüsselt als Voreinstellung») mitbauen oder bewusst ausnehmen | S–M | Claude, zusammen mit E10 |
| K35 | **Regel für PRs, die nachgeschärft werden:** sofort auf Entwurf (`gh pr ready --undo`). Heute zweimal im alten Stand gemergt (#167→#168, #169→#170), und §8.7 fehlte seit dem 15.09. aus demselben Grund | — | Claude |

---

## 11 · Stand 16.09.2026, abends — die Entscheide aus §10 gebaut

Vier Arbeitskopien parallel, je ein PR, zuerst als Entwurf (K35). Vor dem Bereitmachen alle vier
probeweise zusammengeführt: 1124 Tests grün, Build grün, **Hauptbundle 64.94 von 65 kB**. Einziger
Konflikt: eine Importzeile in `Dashboard.jsx` zwischen #174 und #175, im Zweig von #175 gelöst.
Sichtprüfung im Browser mit Testdaten: #173 hell, dunkel und 400 px; #175 Dashboard nach dem Merge.

| # | Stand | PR |
|---|---|---|
| E27 | gemergt | #172 |
| E10 · K34 | gemergt. Die automatischen Schnappschüsse bleiben bewusst unverschlüsselt: sie entstehen ohne Eingabe, ein Schlüssel läge im selben Browser | #173 |
| E17 · E18 | gemergt. «Trifft nicht zu» bei Telefon, E-Mail und den Arbeitsfeldern; Löschweg in den Einstellungen, der Beta-Zugang bleibt erhalten | #174 |
| K13 (erster Teil) · K18 · K26 · K27 · O14 | bereit, CI grün. Der Walliser Wert ist unverändert (Rückfrage offen) | #175 |

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K36 | **Bundle-Reserve aufgebraucht** (0.06 kB). Die nächste Änderung im Hauptbundle bricht den Grössen-Check. Platz schaffen (Teile aus dem Hauptbundle in lazy Chunks) oder das Limit bewusst anheben → E36 | S–M | Claude, nach E36 |
| K37 | **K13, zweiter Teil:** Für den Kantons-/Gemeindesteuer-Faktor ist im Code nur ein Eichpunkt belegt (etwa CHF 80'000, ledig; `kantonaleSteuerdaten.js:3`), kein Einkommensband. Ohne Band keine Zurückhaltung → E37 | S | Entscheid |
| K38 | Rückfragen aus #174: «trifft nicht zu» auch beim Notfallkontakt? · Markierung beim Arbeitgeber zwischen «Finanzen» und «Ausbildung & Arbeit» koppeln? · Alimente ohne Kinder automatisch als «trifft nicht zu»? · `_na` erscheint im CSV-Export als eigene Zeile, und die Export-Vorschau zählt ein Kapitel nur mit `_na` als «hat Angaben» | S je Punkt | Entscheid, dann Claude |
| K39 | Rechts-Runde: Datenschutzerklärung und App-Text sagen bei der Sicherung weiter «auf Wunsch verschlüsselt» (`datenschutzerklaerung-ndsg.md` Z. 53/108, 5 Sprachen); `legal.privacy.rights2` nennt den neuen Löschweg nicht; reicht ein lokaler Löschweg ohne Protokoll? | S | Rechts-Prüfer, dann Claude |
| K40 | `export.securityNote` (de) steht in der Du-Form («Bewahre sie sicher auf») und erscheint auch in der Sie-Ansicht. Stand schon vor dieser Runde auf `main` | S | Claude |
| K41 | Berg-Beschriftungen noch nicht begonnener Kapitel stehen mit Deckkraft 0.5 auf der grauen Bergfläche (vorher 9 px, jetzt 13 px). Kontrast nachrechnen, vermutlich unter 4.5:1 | S | a11y-Prüfer |

**Neue Entscheide**

| # | Frage | Empfehlung |
|---|---|---|
| E36 | K36: Platz im Hauptbundle schaffen oder das Limit anheben? | Platz schaffen. Das Limit ist die einzige Bremse gegen schleichendes Wachstum |
| E37 | K37: welches Einkommensband für den Steuerfaktor? | Kein Band erfinden. Entweder aus den ESTV-Tabellen je Kanton eichen (Oktober) oder bei der Kennzeichnung «grobe Schätzung» bleiben |

**Weiter rückgefragt:** E1 (wageClaim) und der Walliser Taxpunktwert. Die Wortwahl «Rumantsch
(provisoric)» gehört zur Gegenlese K29.

---

## 12 · Stand 16.09.2026, spätabends — dritte Runde

**Entschieden am 16.09. abends:** E36 Platz schaffen (Limit bleibt 65 kB) · E37 jetzt eichen ·
E38 Kantonssteuer als Stütztabelle je Kanton, jetzt, mit 1–3 Kindern nachmessen · E1 bleibt
geparkt, **im Oktober neu anschauen** · Walliser Taxpunktwert: **warten** · K38: Notfallkontakt
«trifft nicht zu» ja, mit Anregung, jemanden zu fragen; Arbeitgeber koppeln; Alimente selbst markieren.

| # | Stand | PR |
|---|---|---|
| E36 · K36 | gemergt. Hauptbundle 64.94 → 61.00 kB | #177 |
| K39 | gemergt. Sieben Rechtsfragen für eine juristische Prüfung im PR; die zitierten Artikel sind nicht gegen Fedlex geprüft | #178 |
| K38 | gemergt | #179 |
| K40 · K41 · K33 | gemergt. Die Steuerkanton-Vorbelegung liegt in `src/utils/steuerkanton.js` (vorher Zirkel-Import über den Steuerrechner) | #181 |
| E37 · K37 | gemergt. **Befund:** der Faktor lag bei CHF 80'000 brutto in allen 26 Kantonen 44–70 % zu tief | #180 |
| E38 | gemergt. 208 Reihen (26 Kantone × ledig/verheiratet × 0–3 Kinder), 14'144 ESTV-Messpunkte, Abweichung im Median CHF 5, höchstens CHF 391 | #182 |
| E39 | in Arbeit: Bundessteuer mit demselben steuerbaren Einkommen | — |

Alle Messungen am ESTV-Steuerrechner laufen über `scripts/steuerband-messen.mjs`, seriell mit 150 ms
Pause; die Rohwerte liegen unter `docs/sources/*.messpunkte.json`.

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K42 | **Bundessteuer zu hoch** (Fund aus #182): sie wird aus dem Nettolohn ohne die ESTV-Standardabzüge gerechnet — ZH ledig 1'022 statt 906, ZH alleinerziehend mit 1 Kind 447 statt 114, VD verheiratet mit 2 Kindern 1'670 statt 551. Auch live | S–M | Claude (E39) |
| K43 | Beim ersten Wechsel auf eine noch nicht geladene Sprache (fr, rm) blitzen rohe Schlüssel auf (`src/i18n/index.js`: `if (!translations[lang]) return (key) => key;`). Die bisherige Sprache stehen lassen, bis die neue geladen ist | S | Claude |
| K44 | Kantonssteuer auch bei als Brutto erfasstem Lohn (zweite, nach Brutto geordnete Tabelle aus denselben Messdaten) · Nebeneinkommen zählt nur der Steuerrechner · 13. Monatslohn berücksichtigt keine der drei Seiten · BVG-Schwelle aus dem Nettolohn abgeleitet (bis CHF 900 Abweichung zwischen 20'969 und 23'111) | S je Punkt | Entscheid, dann Claude |
| K45 | Rückfragen aus #179: «Arbeitsbeginn» und «Anstellung seit» koppeln? · Pflicht-Sternchen beim Notfallkontakt entfernen? · soll ein eingetragener Wert die Markierung «trifft nicht zu» aufheben? | S je Punkt | Entscheid |
| K46 | ✅ **entschieden 17.09. «alle nachziehen», gebaut (§17).** — Ursprünglich: Sie/Du: der Tresor-Bildschirm (`tresorLock`) steht nur in der Du-Form (dormant, siehe E2); Lebensereignis-Anleitungen duzen breit | S | Entscheid |
| K47 | Lizenzliste in der App ohne Atkinson Hyperlegible und Capacitor · «gemeinnützig» nicht belegt · Umweltangaben zu Infomaniak ohne Quelle · «vollständig offline» gilt erst nach dem ersten Laden (aus #178) | S | Claude, Rechts-Prüfer |
| K48 | Juristische Prüfung der sieben Fragen aus #178 (u. a. lokaler Löschweg ohne Protokoll, unverschlüsselte Schnappschüsse und DSG Art. 7 Abs. 3, Restrisiko «gering» oder «mittel», Impressum-Adresse E12) | — | Stebler Studios |

**Zum Deploy:** Live läuft 0.1.28-beta. Gemergt und nicht live sind 0.1.29-beta und #172–#182.
Empfehlung: erst E39 mergen, dann **ein** Deploy, damit Kantons- und Bundessteuer zusammen richtig
live gehen.

---

## 13 · Stand 17.09.2026, früh — 0.1.29-beta live, vierte Runde

**Deploy 17.09.2026, 01:46** (Hand von Stebler Studios) aus `2fcf409`, Tag `v0.1.29-beta`. Per `curl`
belegt: `index-91c30770.js`, alle 159 Build-Dateien 200, altes Bundle `index-2301b4b1.js` 404,
erfundener Name 404, Merkmale von #173–#185 im ausgelieferten Code gefunden (Gegenprobe 0), Backup
`20260917-014608` mit 165 Dateien. Vorher: Prüf-Batterie mit 9 Prüfern über `2aaeff5..0c9cc43`,
kein Blocker; die «sollte»-Punkte in #185.

| # | Stand | PR |
|---|---|---|
| E39 · K42 | live | #184 |
| Predeploy-Befunde (PII-Scan, Tippflächen, fr, KVG-Artikel) | live | #185 |
| R4 Hinweise und Robustheit | gemergt, nicht live | #186 |
| R4 Steuer-Annahmen | gemergt, nicht live | #187 |
| R4 «öffnet in neuem Tab» (38 Links) · fr/it `legal.*` mit Sie/Du | gemergt, nicht live | #188 |

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K49 | ✅ **erledigt 17.09. (#203, live 0.1.32-beta):** 237 fr + 119 it umgestellt, Liste der Lücken leer. Gegenlesen durch Muttersprachler:innen offen. — Ursprünglich: Sie/Du in fr/it: rund 356 Schlüssel haben nur eine Anredeform (als `KNOWN_GAPS` im Test aus #188 geführt; neue Lücken schlagen an) | M | Claude, dann Gegenlesen |
| K50 | DBG Art. 33 Abs. 1bis: fällt der erhöhte Versicherungsabzug bei Säule-3a-Beiträgen weg? Fedlex liess sich maschinell nicht lesen | S | Stebler Studios (Gesetzestext) |
| K51 | Sozialhilfe zeigt «CHF 10.000» statt «10'000» · Freibetrag-Hinweis auch im Anspruchs-Check? · Kantonsportale GL, SZ, BL, FR maschinell nicht prüfbar | S | Entscheid, dann Claude |
| K52 | KVG-Fussnote: den richtigen Artikel für die provisorische Festsetzung der Taxpunktwerte (vermutlich Art. 47 KVG) an Fedlex belegen und wieder nennen | S | Claude, Rechts-Prüfer |
| K53 | Aus dem Predeploy-Gate: `export`-Knopf deaktiviert 3.4:1 (von AA ausgenommen) · `radius.full` nie genutzt · Druckvorlagen mit festen Hex-Farben · Quellen-Kommentar zur Berufsauslagen-Pauschale | S | Claude |

## 14 · Stand 17.09.2026, vormittags — fünfte Runde

**K52 geklärt, ohne Code-Änderung.** Das KVG (SR 832.10, Fedlex, Stand am 1. Juli 2026) am
17.09. im Browser gelesen:

- **Art. 46 Abs. 4:** Die Kantonsregierung oder der Bundesrat genehmigt Tarifverträge. Von «provisorisch» ist keine Rede.
- **Art. 47 Abs. 1 und 3:** Kommt kein Vertrag zustande, setzt die Kantonsregierung den Tarif fest. Sie kann einen bestehenden Vertrag um ein Jahr verlängern. Auch hier kein «provisorisch».
- **Im ganzen Gesetz** kommt «provisorisch» nur bei der Regelung zu Art. 65 vor (Prämienverbilligung, Art. 97 Abs. 2 und eine Übergangsbestimmung).

Die vorläufige Festsetzung der Taxpunktwerte 2026 steht also nicht in einem KVG-Artikel. Der
Hinweis `kvg.tpwNote` bleibt deshalb ohne Artikelnummer, wie seit #185. Art. 47 dort zu nennen,
würde eine gesetzliche Grundlage fürs «Provisorische» vorspiegeln, die es im Wortlaut nicht gibt.
K52 ist damit erledigt.

| # | Stand | PR |
|---|---|---|
| K43 | Sprachwechsel erst nach dem Laden der neuen Sprache, keine rohen Schlüssel mehr (8 neue Tests) | #194 |
| K47 | Lizenzliste um Atkinson Hyperlegible, Capacitor und jsQR ergänzt · «gemeinnützig» gestrichen · Infomaniak-Angaben auf den belegten Wortlaut gekürzt, mit Quelle · «vollständig offline» präzisiert (5 Sprachen) | #196 |
| K52 | geklärt, siehe oben | dieser PR |
| K53 | gesperrte Knöpfe ≥ 4.5:1 mit Form-Signal · `radius.full` kommentiert, nicht gelöscht · Druckfarben im Dossier als Konstante `DRUCK`, Ausgabe bytegleich · Quelle der Berufsauslagen-Pauschale (SR 642.118.1 Art. 7) | #195 |

Die drei Zweige wurden zusammen getestet (Probe-Merge auf `7fc1439` plus #194–#196, ohne
Konflikt): 1763 Tests grün, Build und Lint ohne Befund, Hauptbundle 61.62/65 kB. Auf GitHub sind
alle drei CI-Läufe grün.

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K54 | Die Berufsauslagen-Pauschale wird bei Teilzeit oder unterjähriger Anstellung nicht gekürzt. SR 642.118.1 Art. 7 Abs. 2 verlangt eine «angemessene» Kürzung. Vor dem Bau klären, wie die ESTV kürzt (Quelle), dann Swiss-Precision-Prüfer | S–M | Claude, nach Beleg |
| K55 | ✅ **entschieden 17.09. «offline-fähig», gebaut (§17).** — Ursprünglich: Werbetexte «100 % privat, offline, kostenlos» (`index.html`, `manifest.json`, App-Texte) → «offline-fähig»? · FAQ-Satz «Keine Daten werden je über das Netz gesendet» prüfen | S | Entscheid, dann Claude |
| K56 | Lizenzen: jsQR (Apache-2.0) wird ohne Lizenzkopie ausgeliefert · MIT-Hinweis von QRCode.js fällt im Build vermutlich weg · Satz «alle Lizenzen AGPL-3.0-kompatibel» für OFL ungeprüft · App Store und AGPL vor einem iOS-Release klären | S | Claude (erste zwei), Rechts-Prüfer |
| K57 | Doku-Reste: «Daten bleiben in der Schweiz (nDSG-konform)» ohne Beleg · «Vercel Speed Insights» in `privacy-security-position.md` veraltet | S | Claude |
| K58 | ✅ **entschieden 17.09. «Deutsch», gebaut (§17);** Ladehinweis-Frage bleibt offen. — Ursprünglich: Rückfragen aus #194: Rückfall-Sprache Englisch oder Deutsch? · kein Ladehinweis und keine Meldung bei einem Ladefehler, reicht das? | S | Entscheid |

## 15 · Stand 17.09.2026, 11:10 — 0.1.30-beta live, Befunde aus der Vorab-Prüfung

**Deploy 17.09.2026, 10:55** (Hand von Stebler Studios) aus `b20cd0b`, Tag `v0.1.30-beta`. Per `curl`
belegt: `index-98d2d140.js`, alle 159 Build-Dateien 200, altes Bundle 404, erfundener Name 404,
Merkmale von #186–#198 im ausgelieferten Code. Damit ist nichts mehr gemergt und nicht live.

**Vorab-Prüfung** (9 Prüfer über `2fcf409..0b467da`): ein Blocker, nämlich die Version war nicht
angehoben (behoben in #198). Dazu hat #198 übernommen: Löschweg-Frist für andere Tabs (von 4 Prüfern
gemeldet), Druckfarbe «Annahmen», Freigabe-Register ohne KVG-Artikel. Die übrigen Befunde:

| # | Punkt | Quelle | Grösse | Hand |
|---|---|---|---|---|
| K59 | ✅ **erledigt 17.09. (#200, live 0.1.31-beta):** gemessen, Befund bestätigt (offline leer), behoben und lokal ohne Server nachgemessen. — Ursprünglich: **Offline-Aussage:** Der Service Worker legt beim ersten Besuch nur `/`, `theme-init.js` und zwei Schriften ab, das Haupt-Skript unter `/assets/` nicht (`public/sw.js:26-35`, `:60-73`); nach jedem Deploy leert `activate` den Speicher. «Nach dem ersten Laden auch offline» ist damit vermutlich noch zu stark. Im Browser prüfen (offline schalten nach dem ersten Besuch), dann entweder den Speicher-Weg ändern oder den Text | Rechts-Prüfer | S–M | Claude, mit Messung |
| K60 | ✅ **erledigt 17.09. (#204, #208, live 0.1.32-beta).** — Ursprünglich: **Weisse Seite**, wenn beim ersten Start auch die Rückfall-Sprache nicht lädt (`src/i18n/index.js:133-137`, `:164`, `return null`); nach einem Deploy möglich, solange eine alte `index.html` im Cache liegt (O20 fehlt live). Ruhiger Fehlerzustand mit «Neu laden» | 5 Prüfer | S | Claude |
| K61 | ✅ **erledigt 17.09. (#204, #208, live 0.1.32-beta),** inkl. leere Bereiche und Auto-Sicherung (`utils/prerestore.js`). — Ursprünglich: **Wiederherstellen:** Scheitert der Schnappschuss mittendrin (Speicher voll), sind die `_prerestore`-Schlüssel gemischt aus zwei Ständen (`src/utils/backupCrypto.js:216-226`, `:294`). Erst alle lesen, dann schreiben, bei Fehler zurückrollen | Qualität | S | Claude |
| K62 | **Partner-Regeln:** Die Kantonssteuer verlangt bei jedem Partnereinkommen > 0 «keine Zahl» (`kantonaleSteuerdaten.js:201`), das steuerbare Einkommen nur bei «verheiratet» oder mit Kindern (`:241`) → Konkubinat ohne Kinder bekommt Bundes-, aber keine Kantonszahl · `partnerIncome` 0 erscheint als leeres Feld (`ChapterView.jsx:321`), zählt aber als Antwort · das Feld erscheint nur bei `adultCount >= 2` (`:316`) · Steuerrechner widerspricht sich bei «verheiratet» mit direkt eingetragenem steuerbarem Einkommen (`TaxCalculator.jsx:301-318`) · Tarifvergleich rechnet im Konkubinat «verheiratet» als Alleinverdiener (`:329`) | Sicherheit, Ordnung, Swiss Precision, a11y | S je Punkt | Entscheid, dann Claude |
| K63 | ⚠️ **teilweise:** K49 deckt nur Schlüssel ab, die in de schon Sie/Du haben. Offen: `legal.privacy.sensitive1` (fr), `legal.resources.localGovDesc` (fr, it), `kvg.mammoGeoYourCantonNo` (it in Du-Form, de einheitlich) → siehe K68. — Ursprünglich: **Sie/Du fr/it:** `legal.privacy.sensitive1`, `tax.netIncomeNote`, `bandNotCheckedBrutto`, `federalNotCheckedBrutto`, `legal.resources.localGovDesc` (fr, it), `kvg.mammoGeoYourCantonNo` (it), `legal.faq.a4` (rm), «Complète» → «Complète/indique» · fällt unter K49 | Copy, Swiss Precision | S | Claude |
| K64 | ✅ **erledigt 17.09. (#206, live 0.1.32-beta).** — Ursprünglich: **Barrierefreiheit:** `PremiumSubsidy.jsx:70`, `:351-354` öffnet mit `window.open` einen neuen Tab ohne Ansage und nutzt noch den alten Stil für gesperrte Knöpfe · `ChapterView.jsx:95` zeigt ohne `t` einen rohen Schlüssel · `renderSource.js:11-14` verliert ohne `t` die Ansage still | a11y, Link-Checker, Rechts-Prüfer | S | Claude |
| K65 | ✅ **erledigt 17.09. (#205, #208, live 0.1.32-beta):** «in den meisten Kantonen» (15 von 17 Quellen), ohne Klammer; Links klickbar. rm-Gegenlesen offen (K69). — Ursprünglich: **Quellen:** `kvg.tpwNote` sagt «fast überall provisorisch» ohne Quelle (seit der Artikel weg ist; Quelle wären die kantonalen Beschlüsse) · `legal.ethics.sustainSource` nennt Domains ohne `https://` und ist deshalb nicht klickbar · Rätoromanisch: `TODO(rm)` in `rm.js:1648`, `:1791`, `:2918` | Swiss Precision, Copy, Ordnung | S | Claude, rm gegenlesen |
| K66 | ✅ **erledigt 17.09. (#207, live 0.1.32-beta),** Behörden-JSON bewusst nicht (E40). — Ursprünglich: **Aufräumen:** Annahmen-Liste doppelt (`dossierGenerator.js:781-785` und `KantonssteuerOrientierung.jsx:40-43`), im Behörden-JSON als deutscher Klartext (`:907-910`) · `chf` dreifach, eine Kopie zwischen den Imports (`TaxCalculator.jsx:14`) · `_legalViewT` als Modulvariable im Rendern (`LegalView.jsx:10`, `:93`) · Signal-Schlüssel in `main.jsx` als Text statt `LOESCH_SIGNAL` · `vite.config.js:24` mit festem `dist/sw.js` | mehrere | S | Claude |

Der vollständige Bericht der neun Prüfer liegt ausserhalb des Repos, in den Übergabe-Notizen des Studios.

## 16 · Stand 17.09.2026, nachmittags — 0.1.31 und 0.1.32 live

| Version | Live seit | Bundle | Inhalt |
|---|---|---|---|
| 0.1.31-beta | 17.09. 11:14 | `index-c2f8af36.js` | K59 Offline nach dem ersten Besuch (#200, #201) |
| 0.1.32-beta | 17.09. 11:46 | `index-297034b6.js` | K49 (#203), K60/K61 (#204), K65 (#205), K64 (#206), K66 (#207), Befunde der Vorab-Prüfung (#208) |

Beide per `curl` belegt (alle Build-Dateien 200 — bei 0.1.32 sind es 161, neu `prerestore` und
`steuerTexte` als eigene Teile —, altes Bundle 404, erfundener Name 404, Tag = deployter Commit).
Vorab-Prüfung für 0.1.32: 9 Prüfer über `672d8af..13196e0`, 0 Blocker; die gemeinsamen Befunde
sind in #208 behoben (K61 vollständig, Fehlerhinweis in der gewählten Sprache, Taxpunktwert-Klammer).

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K67 | ✅ **gemergt (#211):** gemessen — `config/cantonalData.js` zog über den Freibetrag den ganzen Sozialhilfe-Rechner ins Hauptbundle (Rollup legt ein Modul immer ganz in einen Teil); herausgelöst, 62.71 → 62.09 kB. Weitere Kandidaten laut Messung: `BetaGate.jsx` 2.8 kB, `MobileNav.jsx` 4.2 kB, `IconSystem.jsx` 12.6 kB (je gzip, einzeln gemessen). — Ursprünglich: Hauptbundle 62.62/65 kB — Reserve 2.4 kB. Vor dem nächsten grösseren Zuwachs prüfen, was aus dem Hauptbundle in eigene Teile gehen kann | S | Claude |
| K68 | ✅ **gemergt (#211):** die vier Texte anredefrei, mit Test und Gegenprobe. **Nebenbefund:** rund 158 deutsche Texte stehen nur in der Du-Form (Wortmuster-Messung, Mischformen wie «Ihre/deine» mitgezählt) und erscheinen so auch in der Sie-Ansicht (Voreinstellung), vor allem Lebensereignis-Anleitungen, Notfallkarte, Zusatz-/KVG-Wechsel, Vorsorge-Zukunft → gehört zum Entscheid K46. — Ursprünglich: Anrede-Reste ausserhalb von K49 (siehe K63): vier Texte in de/fr/it als `{ sie, du }` führen oder neutral formulieren; der Test erfasst sie heute nicht, weil de dort nur eine Form hat | S | Claude |
| K69 | Rätoromanisch: 88 Schlüssel mit nur einer Anredeform; `TODO(rm)` bei `communeHint`, `andereFenster`, `ohneZahl*`, `tpwNote` — Gegenlesen durch eine Fachperson | M | Fachperson, dann Claude |
| K70 | fr/it gegenlesen lassen (356 neue Formen aus K49; 14 fr- und 15 it-Paare sind anredefrei und identisch) | M | Muttersprachler:innen |
| K71 | ✅ **gemergt (#211):** Rückfall liest die geladene Sprache (`rueckfallT`), `createTranslator` entfernt. — Ursprünglich: Rückfall in `utils/tRueckfall.js` ist ein leerer Text (Knöpfe ohne Namen, falls je weder `t` noch Kontext da ist) · `createTranslator` in `i18n/index.js` ungenutzt und immer Sie-Form | S | Claude |
| K72 | Panel-Durchgang: ein CSP-Header aus dem Panel (O8) muss `style-src 'unsafe-inline'` erlauben, sonst ist der Fehlerhinweis aus K60 ungestylt | S | Stebler Studios (Panel) |

**Neue Entscheide**

| # | Frage | Empfehlung |
|---|---|---|
| E40 | Behörden-JSON (`calculations.tax.assumptions`, `taxableIncomeBasis`, `cantonalBasis`): deutscher Klartext unabhängig von der Sprache — Kennungen, Übersetzung oder so lassen? | Kennungen plus deutscher Text daneben; ändert das Dateiformat, darum Entscheid |
| E41 | Die kantonalen Quellen der Taxpunktwerte stehen nur als Kommentare in den Daten. In der App anzeigen? | Ja, als Link je Kanton unter der Tabelle — passt zur Quellen-Redlichkeit |

## 17 · Stand 17.09.2026, mittags — Entscheide K46/K55/K58, siebte Runde

**Entscheide von Stebler Studios (Auswahl-Dialog):** K46 «alle nachziehen» · K55 «offline-fähig» ·
K58 «Deutsch» als Rückfall-Sprache.

| # | Stand |
|---|---|
| K67 · K68 · K71 | ✅ live 0.1.33-beta (#211) |
| K46 | ✅ live 0.1.33-beta (17.09. 12:42): 204 Texte als `{ sie, du }` in de/fr/it (88 + 112 aus zwei Arbeitskopien, 4 nachgezogen), dazu der Test «deutsche Texte duzen nur in der du-Fassung» (ohne Beachtung der Gross-/Kleinschreibung, mit Gegenprobe) |
| K55 | ✅ live 0.1.33-beta: «offline-fähig» in `index.html`, `manifest.json`, README, 5 Sprachen; `legal.faq.a3` präzisiert (Belege: CSP `connect-src 'self'`, kein `fetch`/`XMLHttpRequest`/`sendBeacon` in `src/`) |
| K56 | ✅ erste zwei Teile live 0.1.33-beta (Lizenztexte jsQR und QRCode.js unter `/licenses/`, Copyright-Zeilen gegen die Originalprojekte geprüft). Offen bleiben OFL/AGPL und App Store → K76 |
| K57 | ✅ die zwei genannten Stellen; weitere → K77 |
| K58 | ✅ live 0.1.33-beta: `FALLBACK_LANG = 'de'`, Startsprache bleibt `en`; Parität zusätzlich gegen de |

**K54 · Beleg gesucht (nicht gebaut):** Die ESTV wiederholt im Steuermäppchen 2025 «Pauschalabzug für
übrige Berufskosten» nur den Verordnungstext («angemessen zu kürzen», Art. 7 Abs. 2 SR 642.118.1) und
nennt 3 % des Nettolohns, mind. 2'000, höchst. 4'000 — **wie** gekürzt wird, steht dort nicht
(https://www.estv2.admin.ch/stp/sm/berufskosten-de-fr.pdf, abgerufen 17.09.2026). Das Steuerbuch
Solothurn (Stand 08.12.2022) regelt es konkret und sagt «Die Regelung bei der Bundessteuer ist
identisch»: Teilzeit mit Einkommen bis 20'000 → Pauschale 10 % des Nettoeinkommens statt 2'000;
unterjährig → auf Jahresbasis rechnen, dann pro rata
(https://steuerbuch.so.ch/steuern/einkommenssteuer/steuerbare-einkuenfte-aus-unselbstaendiger-erwerbstaetigkeit-und-berufsauslagen/33-nr-4/).
Die Wegleitung Bern nennt keine Kürzungsregel. Die App kennt weder Pensum noch Anstellungsdauer; die
Stütztabelle trifft den ESTV-Rechner, der beides ebenfalls nicht abfragt. → **Entscheid E42:** nur ein
Hinweis «bei Teilzeit/unterjährig kann die Pauschale tiefer sein» (S), oder Pensum/Dauer erfassen und
nach der SO-Regel rechnen (M, braucht zweite Quelle, ESTV-seitig) — Empfehlung: Hinweis.

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K73 | fr/it-Titel mit Anrede, deren de-Text anredefrei ist (it: `kind.step5Title`, `trennung.step5Title`, `betreibung.step3Title`, `fuehrerausweis.step1Title`/`step2Title`, `kvgWechsel.checkIntro`, `neuerJob.reminderNotes`, Linktexte im Imperativ; fr: `kind.step5Title`, `trennung.step5Title`, `kvgWechsel.checkIntro`) → anredefrei oder `{ sie, du }` | S | Claude |
| K74 | ✅ **live 0.1.33-beta:** `a1` wie `a3` präzisiert (5 Sprachen, rm mit Gegenlese über K69). — Ursprünglich: `legal.faq.a1` sagt «Nichts wird an einen Server gesendet» — ähnlich stark wie der alte FAQ-Satz aus K55 | S | Claude |
| K75 | `docs/legal/third-party-licenses.md` nennt DM Sans und Cormorant; ausgeliefert werden Lexend, Hanken Grotesk, Atkinson Hyperlegible | S | Claude |
| K76 | OFL/AGPL-Satz und App Store vor einem iOS-Release klären; `legal.license.thirdParty1` verweist noch nicht auf `/licenses/` | S | Rechts-Prüfer, dann Claude |
| K77 | Weitere Aussagen ohne Beleg: «nDSG-konform» in `docs/proof-of-concept.md:31`, `docs/security/compliance-overview.md:62`; Vercel in `docs/product/PRD.md:34`, `product-memory-registry.md:98`, `docs/status/current-state.md:27`, `docs/ux/beta-test-skript.md:4` | S | Claude, Rechts-Prüfer |
| K70 | (erweitert) fr/it gegenlesen: zusätzlich rund 200 neue Sie/Du-Fassungen aus K46; die Lei-Formen zuerst. Copy-Prüfer: it schreibt die Höflichkeitsform teils klein (`lebenszustaende`), teils gross (`zusatzWechsel`) → eine Linie festlegen; en «100% private, works offline, free» bricht die Aufzählung | M | Muttersprachler:innen |
| K78 | Weitere absolute Datenaussagen: `privacyNote` («100% lokal. Keine Daten verlassen Ihr Gerät.»), `localBadge` — an `faq.a1`/`a3` angleichen, sobald E43 entschieden ist | S | Claude, nach E43 |

**Neuer Entscheid**

| # | Frage | Empfehlung |
|---|---|---|
| E43 | «100 % privat» / «100 % lokal» in Werbetexten (`index.html`, Manifest, App): Rechts-Prüfer hält die absolute Form für angreifbar (UWG Art. 3 Abs. 1 lit. b), weil der Hoster beim Laden technische Daten (IP) verarbeitet — Jurist:in fragen oder umformulieren? | Umformulieren, z. B. «Ihre Eingaben bleiben auf Ihrem Gerät» — belegbar und ohne Juristenfrage |

**Nachgezogen aus der Vorab-Prüfung (Sprache, Recht, Qualität — je 0 Blocker):** `faq.a1` (K74) ·
Datenschutzerklärung ohne «Performance-Metriken» · jsQR als Version 1.4.0 belegt (SHA-256 gleich wie
das npm-Paket) · Nachhaltigkeits-Statement: kein «CDN», Grössen gemessen, erledigte Domain abgehakt ·
Tresor-Text du-Fassung ohne «Sie werden …».

## 18 · Stand 17.09.2026, nachmittags — 0.1.33-beta live, achte Runde

**Deploy 12:42** (Hand von Stebler Studios) aus `db75d59`, Tag `v0.1.33-beta`; Belege im Doku-PR #213.

| # | Stand |
|---|---|
| K73 | ✅ gemergt (#214): it duzte in 14 Einheitstexten (erschien so in der Sie-Ansicht), jetzt anredefrei bzw. `{ sie, du }`; fr-Titel/`checkIntro`/`reminderNotes` anredefrei; `settingsDrawer.accountNote`, `arztkoffer.subtitle`, `nav.sub.arztkoffer` in de/fr/it als `{ sie, du }`. Neuer Test «italienische Einheitstexte duzen nicht» mit Gegenprobe |
| K75 | ✅ gemergt (#214): `third-party-licenses.md` nennt Lexend, Hanken Grotesk, Atkinson Hyperlegible |
| K77 | ✅ gemergt (#214): «nDSG-konform» → «juristisch nicht geprüft (K48)»; Vercel-Angaben in PRD, Registry, Beta-Skript auf Infomaniak/malojaplana.ch (`docs/status/current-state.md` trägt schon einen Überholt-Vermerk und bleibt) |

**Entscheide von Stebler Studios (17.09. nachmittags):**
- **E43 «1 & später im Okt. 2»** → jetzt umformulieren (✅ PR E43/K78: «lokal gespeichert», «Ihre Eingaben bleiben auf Ihrem Gerät», Test gegen «100 % privat/lokal»), im Oktober zusätzlich die Jurist:in fragen (zu K48).
- **E42 «ab Okt. 2»** → ab Oktober Pensum (und Anstellungsdauer) erfassen und die Pauschale gekürzt rechnen; vorher eine zweite, ESTV-seitige Quelle zur Kürzungsweise suchen. Bis dahin nichts bauen.

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K79 | ✅ **gemergt (#215):** fr 57 (10 als `{ sie, du }`, 47 anredefrei; 7 Treffer waren «rendez-vous»), de 6 als `{ sie, du }`, it 34 Du-Verbformen ohne Pronomen anredefrei, dazu `po.compareFranchiseNote`/`langSkill.hint` (de) im Infinitiv; Test «feste Sie-Formen nur in der sie-Fassung». Offen: Verbformen ohne Pronomen erkennt kein Test (Kurz-Knöpfe im Imperativ bleiben bewusst). — Ursprünglich: Feste Sie-Formen, die auch in der Du-Ansicht erscheinen: fr rund 61 Einheitstexte mit vous/votre/vos (ohne Briefe und Rechtstexte; u. a. `dashboard`, `lebenszustaende`, `tax`, `calendar`, `vr`), de rund 9 (Wortmuster, «Sie» als Pronomen «sie» kann mitzählen). Briefe (`briefe.*`) siezen die Stelle, nicht die Person — dort richtig. → wie K46 als `{ sie, du }` oder anredefrei | M | Claude |

## 19 · Stand 17.09.2026, nachmittags — Panel-Durchgang begonnen, Demo-Fehler

**M15 ✅ live:** `permissions-policy: camera=(), microphone=(), geolocation=(self)` auf `/`, `/assets/`,
`/fonts/`, `/sw.js` (curl 17.09.); im Browser erlaubt die Seite den Standort (`allowsFeature('geolocation')`
= true), Kamera bleibt gesperrt; Seite, Stile und Navigation normal, Konsole ohne Fehler. Stebler Studios
hat die **`.htaccess` auf dem Server** angepasst (Sicherung `.htaccess-backup-20260917` daneben) —
die Live-Header kommen also aus der Server-Datei; `deploy.sh` lässt sie unberührt (Wurzel ohne `--delete`).
Offen im selben Durchgang: HSTS 16000000 → 31536000 · CSP-Header mit `frame-ancestors 'none'` und
`style-src 'unsafe-inline'` (O8/K72) · Cache 1 Jahr für `/assets/` und `/fonts/` (O20).

**Fehler beim Messen gefunden und behoben (Release 0.1.34-beta):** «Beispiel verlassen» im Banner
reichte das Klick-Ereignis an `demoVerlassen` weiter → Adresse `/NaN`, `reload is not a function`,
Demo nicht verlassbar (live in 0.1.33-beta). Lesson: Funktionen mit optionalen Parametern nie direkt
als `onClick` übergeben.

**Nachtrag 17.09., 13:40 — 0.1.34-beta live** (13:25, `index-e5ece27b.js`; Demo-Fehler behoben, live im
Browser geprüft). **HSTS ✅** genau ein Header `max-age=31536000`. Neu gemessen: `http://malojaplana.ch/`
antwortet 200 ohne Weiterleitung auf https, `www.` ohne Weiterleitung auf die Hauptadresse → Panel
(«HTTPS erzwingen», Weiterleitung), Hand von Stebler Studios. Brain-Dump Speicherorte (kDrive, Proton
Drive, SecureSafe) und offene Karten → `docs/IDEEN.md` §14, Oktober oder später.

## 20 · Stand 17.09.2026, Nachmittag — Header-Durchgang und CSP-Prüfbatterie

Alle Header kommen aus der **Server-`.htaccess`**, von Stebler Studios von Hand gesetzt und jeweils gesichert.
Nach jeder Änderung per `curl` und im Browser nachgemessen.

| Punkt | Stand | Beleg (17.09.2026) |
|---|---|---|
| M15 Standort | ✅ | `permissions-policy: camera=(), microphone=(), geolocation=(self)`; `allowsFeature('geolocation')` = true, Aufruf nur auf Knopfdruck (`NotfallVorlesekarte.jsx:49`) |
| HSTS | ✅ | genau ein `strict-transport-security: max-age=31536000` auf allen Pfaden inkl. 404 und `www.` |
| http → https | ✅ | `http://malojaplana.ch/` → **301** → `https://malojaplana.ch/`, Pfad und Parameter bleiben (`curl --max-redirs 0`) |
| www → Hauptadresse | ✅ | `http://www.` und `https://www.` → je ein **301** → `https://malojaplana.ch/` |
| O8/E19/K72 CSP-Header | ✅ | genau **ein** `content-security-policy` auf `/`, `/sw.js`, `/theme-init.js`, `/assets/…`, `/fonts/…`, `/manifest.json`, 404; Wert zeichengleich mit der Meta-CSP plus `frame-ancestors 'none'`; kein Report-Only |
| O20 Cache-Control | ⏳ offen | nächster Schritt: nur `/assets/` und `/fonts/` 1 Jahr `immutable`, `index.html`/`sw.js`/`theme-init.js` `no-cache` |

**CSP-Prüfbatterie im Browser** (live, Beispiel-Modus, localStorage vorher und nachher leer; Aufpasser auf
`securitypolicyviolation` plus Konsole): **0 Verletzungen** über Code-Wand, Beispiel öffnen, 11 Ansichten
(Steuern, Budget, Vorsorge, Notfallkarte, KK, Sozialhilfe, Kalender, Rechtliches, Flyer, Organspende,
Notfall-Dossier), Hell/Dunkel, QR erzeugen und mit `jsQR` lesen, Beispiel verlassen. Service Worker aktiv,
Cache 55 Einträge (Startseite, Haupt-Skript, CSS, Sprache de, 6 Schriften); Manifest ok; Lexend und Hanken
Grotesk geladen. **Clickjacking:** Einbettung von fremder Adresse (`localhost:4341`) bleibt leer, ein
Kontrollrahmen ohne Sperre zeigt Inhalt. JSON-LD: gültig, keine Meldung. Meta-CSP und Header zusammen
blockieren nichts zusätzlich. Der QR-Scanner nutzt nur hochgeladene Bilder — `camera=()` stört ihn nicht.

**Neu offen**

| # | Punkt | Grösse | Hand |
|---|---|---|---|
| K80 | ✅ **live 0.1.36-beta (17.09. 16:32, #226, #227).** Ursache grösser als angenommen: schon ein Umlaut verfälschte die Kodierung (Bibliotheksfehler), betraf auch Organspende und KK-Karte. — 🔴 **QR im Notfall-Dossier fehlt bei längeren Angaben.** `NotfallDossier.jsx:17–31` gibt bis zu 1200 Zeichen an QRCode.js (Fehlerkorrektur H); bei Umlauten/Zeilenumbrüchen wirft die Bibliothek «code length overflow» bzw. einen TypeError, der `catch {}` verschluckt ihn → leere Fläche ohne Hinweis. Live im Beispiel-Datensatz nachgestellt; direkt: 1200 Zeichen gemischt → Fehler, 700 × «ä» → Fehler, 1200 × «a» → ok. Vorschlag: Text an die tatsächliche Kapazität anpassen (Bytes statt Zeichen zählen, Fehlerkorrektur tiefer), bei Fehlschlag ruhiger Hinweis; Test mit langem Umlaut-Text. Prüfen, ob `OrganDonation.jsx` und `FlyerView.jsx` dieselbe Lücke haben | S | Claude |
| O20 | Cache-Control, siehe oben | S | Stebler Studios (Server), Claude misst |

## 21 · Stand 17.09.2026, später Nachmittag — Entscheid-Runde und Release 0.1.35-beta

**Entscheide von Stebler Studios (Auswahl-Dialog, 17.09.2026):**

| # | Entscheid | Stand |
|---|---|---|
| E40 | Feste Kennung **plus** Text in der gewählten App-Sprache | gebaut #221 |
| E41 | Quellen-Link je Kanton | gebaut #221 |
| K45 | alle drei: koppeln · Sternchen weg · Eintrag hebt «trifft nicht zu» auf | gebaut #220 |
| K58 | Ladehinweis + Meldung beim Ladefehler (Rückfall Deutsch war schon gebaut) | gebaut #219 |
| K62 | **nur Punkt 2 und 4 jetzt**, Punkte 1, 3, 5 im Oktober | 2+4 gebaut #220/#221 |
| E1 · E3 | Oktober (bestätigt) | — |
| Studio-Website | `noindex` bleibt bis Kundenfreigaben und Rechtsprüfung → Oktober | — |
| O20 | ✅ live gemessen: `/assets/` + `/fonts/` `public, max-age=31536000, immutable`; `/`, `index.html`, `sw.js`, `theme-init.js`, `manifest.json`, 404 ohne. Der `no-cache`-Teil für den Einstieg kommt **nicht** an → empfohlen, kein Blocker | Server-`.htaccess` |

**Gemeinsamer Test Release-Zweig:** 1916 Tests, Build, Lint, PII grün; Hauptbundle 63.51 kB (Reserve 1.49 kB).
**Verdacht widerlegt:** Arbeitgeber-Spiegelung (`main.jsx:721–733`) schneidet beim Tippen nicht ab — im Browser Taste für Taste «Muster AG» getippt, das andere Kapitel zeigt den vollen Wert.

**Neu offen**

| # | Punkt | Hand |
|---|---|---|
| K81 | Notfallkontakt: in der Grundordnung lassen oder nur «empfohlen»? (Rückfrage #220) | Entscheid |
| K82 | Gespeicherte 0 in Währungsfeldern: als Antwort zählen und sichtbar? (Rückfrage #220) | Entscheid |
| K83 | K62.4 Fall 1: Bundessteuer weiter zeigen oder «lieber keine Zahl»? (Rückfrage #221) | Entscheid |
| K84 | rm-Fassung der Sprach-Ladefehler-Meldung und der neuen E40-Texte (`TODO(rm)`) | Fachperson (mit K69) |
| K85 | `no-cache` für `/`, `index.html`, `sw.js`, `theme-init.js`, `manifest.json` in der Server-`.htaccess` | Stebler Studios (Server) |
| K86 | Effektiver Steuersatz bei «verheiratet» + direkt eingetragenem gemeinsamem Wert bezieht sich auf den eigenen Nettolohn (`steuerRechner.js:253/258`, `TaxCalculator.jsx:303`) → weglassen oder auf gemeinsamen Wert beziehen; Zeile «Bruttoeinkommen» als eigener Nettolohn kennzeichnen (Swiss Precision ⚠️, schon vorher so) | Claude |
| K87 | Probiermodus: abweichende Kinderzahl mit direkt eingetragenem Wert → wie K62.4 keine Zahl (`TaxCalculator.jsx:70/188`; ledig 50 000, 2 Kinder → 400.80 ohne Hinweis) | Claude |
| K88 | Quellen-Links: Versicherergruppe anzeigen, wo der Beleg nur für eine gilt (GE CSS · VD santéservices · LU tarifsuisse · UR HSK); Stand-Datum bei GR/ZG-Übersichten | Claude |
| K89 | FR-Quelle `bdlf.fr.ch/…/842.1.24` ist eine JS-Anwendung (jede Adresse 200) → im Browser bestätigen; TI-Quelle ist ein ganzes Amtsblatt (Beschluss ab ~S. 13) → direktere Quelle oder Seitenangabe | Claude / Stebler Studios |
| K90 | Sprach-Ladefehler-Karte steht im DOM ganz am Ende (Tab-Reihenfolge ≠ Sichtposition) | Claude, klein |

**Vorab-Prüfung 0.1.35 (Swiss Precision, Links, a11y/Sprache): 0 Blocker.** Übernommen: Fokus nach Retry/Schliessen (auf Desktop und Handy im Browser geprüft), Tippflächen der Quellen-Links (44 px, im Browser gemessen), Hinweis «gemeinsamer Wert». 1916 Tests, size-limit 63.32/65 kB.

## 22 · Stand 17.09.2026, 14:45 — 0.1.35-beta live

**Deploy 14:35** (`index-d179693a.js`, Tag `v0.1.35-beta` = `cb29a33`): 164/164 Dateien 200, altes Bundle 404, Gegenprobe 404, im Browser geprüft (Beispiel → KVG-Quellen 17 Links, Konsole leer). **O20 ✅** (siehe §21). Deploy-Gate: Sicherheit (ganze App), Qualität, Recht/Copy je 0 🔴.

**Neu offen aus dem Deploy-Gate**

| # | Punkt | Hand |
|---|---|---|
| K91 | ✅ **live 0.1.36-beta (17.09. 16:32, #224).** — `kvg.tpwNote` (de/fr/it/rm): «amtlich belegter Wert» → widerspricht der Beschriftung «Ärztegesellschaften» für OW/NW/SZ; umformulieren | Claude, 0.1.36 |
| K92 | ✅ **live 0.1.36-beta (17.09. 16:32, #224).** — SZ 0.85 gilt nur für tarifsuisse/santéservices, CSS/HSK 0.86 — die App sagt es nicht (Fussnote oder Quellen-Zusatz, zusammen mit K88) | Claude, 0.1.36 |
| K93 | ✅ **live 0.1.36-beta (17.09. 16:32, #224).** — Behörden-JSON ohne Disclaimer → `disclaimer: { code: 'orientierung', text }` aus `behoerdenDossier.disclaimer` (anredefrei) | Claude, 0.1.36 |
| K94 | Speicherzugriff ohne `try/catch`: `src/utils/docReminders.js:14` (`saveReminders`), `src/Lebenssituationen.jsx:51` | Claude |
| K95 | `npm audit`: Lücken nur in Entwicklungs-Paketen (esbuild ≤0.24.2, postcss, nanoid u. a.); Laufzeit 0 → eigener Zweig | Claude |
| K96 | `@capacitor/core`/`ios` als Laufzeit-Abhängigkeit, im Web ungenutzt → verschieben oder begründen | Entscheid |
| K97 | Escape-Funktionen (`helpers.js:51`, `dossierGenerator.js:86`, `briefGenerator.js:103`): `0` wird leer, `'` unmaskiert (nicht sicherheitsrelevant) | Claude |
| K98 | Texte: «Ohne Partnereinkommen im Profil 0 eintragen» doppeldeutig · it «qui sopra», «salario/stipendio netto» einheitlich · «gemeinsam» nur als Annahme formulieren · eingetragene Partnerschaft fehlt in den Zivilstand-Optionen | Claude / Entscheid |
| K99 | Individualbesteuerung: Inkrafttreten bei der ESTV prüfen, bevor «gemeinsamer Wert» als dauerhaft gilt | Stebler Studios / Claude |

## 23 · Stand 17.09.2026, 16:37 — 0.1.36-beta live

**Deploy 16:32** (`index-c80eed98.js`, Tag `v0.1.36-beta` = `6546358`): 164/164 Dateien 200, altes Bundle 404, Gegenprobe 404, `index.html`, `theme-init.js`, `sw.js`, `sitemap.xml` zeichengleich mit dem Build; im Browser (Beispiel) den Notfall-QR mit jsQR zurückgelesen. Umfasst K80 (#226), K91–K93 (#224) und die Gate-Korrekturen (#227).

**Deploy-Gate 0.1.36:** Sicherheit und a11y über die ganze App, Swiss Precision, Qualität, Recht und Wortlaut über den Diff — je 0 🔴. Übernommen: Kürzen füllt weiter und der Code nennt Fehlendes · Medizin zuerst im QR (Entscheid Stebler Studios) · kein Klartext-`title` am QR, `role="img"` + `aria-label` · Emoji als 4-Byte-UTF-8 · Kürzungs-Hinweis ohne Live-Region · QR-Hinweis «nicht verschlüsselt, Bildschirmfoto oder Seite drucken» · en «responsible authority». Nicht gelaufen: Design-Kritik, `/simplify`.

**Neu offen**

| # | Punkt | Hand |
|---|---|---|
| K100 | Notfall-QR: Antworten wie «Noch nicht» (Vorsorge) weglassen, damit z. B. der Hausarzt Platz hat? Im Beispiel fällt er heute weg. | Entscheid |
| K101 | Hinweis «für alle lesbar, die scannen» auch am KK- und Organspende-QR (schon vor 0.1.36 so) | Entscheid, dann Claude |
| K102 | «tarifsuisse» heisst heute santéservices: Linktext SZ als «santéservices (vormals tarifsuisse)» | Claude, klein |
| K103 | `TAXPUNKTWERT[canton] \|\| 0.89` rechnet bei unbekanntem Kanton still mit 0.89 (`KVGLeistungen.jsx:359`, `kvgLeistungen.js:206`) | Claude, prüfen |
| K105 | Der Zugangscode der geschlossenen Beta steht im Klartext in einer getrackten Doku-Datei (`docs/ux/beta-test-skript.md`); das Repo ist öffentlich, der Hash im Code schützt ihn also nicht. Gefunden am 10.09., seither nicht entschieden: Code wechseln und Datei bereinigen, oder bewusst so lassen (weiches Client-Gate). | Entscheid |
| K104 | Deploy-Wächter (`.claude/settings.json`, lokal) suchte die Marke im Ordner `maloja plana` statt im Repo → am 17.09. korrigiert und mit Attrappe geprüft (keine/falsche Marke abgelehnt, passende durchgelassen). Die Datei liegt in keinem git. | ✅ erledigt |

## 24 · Stand 19.09.2026 — Fahrplan, Entscheid-Block, Release 0.1.37-beta

**Fahrplan bis 30.09. angenommen** (im Studio-Repo `FAHRPLAN-bis-2026-09-30.md`), mit zwei Fertig-Definitionen (Maloja + Studio-Website), «kürzen erlaubt, ergänzen nicht». Neues geht in den Oktober-Kasten.

**Entscheide von Stebler Studios (Auswahl-Dialog, 19.09.2026):**

| # | Entscheid | Stand |
|---|---|---|
| K105 | **Bis Oktober bewusst gelassen.** Begründung: weiches Client-Gate ohne Serverdaten — wer den Code kennt, kommt früher in die Beta, sieht aber keine Daten anderer (alles lokal im Browser). Wechsel des Codes (neuer Hash) oder Abschaffen des Tors wird im Oktober entschieden. | Oktober |
| K81 | Notfallkontakt bleibt in der Grundordnung (ohne Stern, «trifft nicht zu»); im Oktober nochmals ansprechen | Oktober |
| K82 | Gespeicherte 0 in Betragsfeldern zählt und wird als «0» gezeigt | gebaut 0.1.37 |
| K83 | Bundessteuer bei offenem Partnereinkommen + gemeinsamem Wert weiter zeigen; im Oktober nochmals anschauen | Oktober |
| K96 | Capacitor → devDependencies | gebaut #230 |
| K99 | **Belegt:** Individualbesteuerung tritt 2032 in Kraft (Bundesrat 19.08.2026, ESTV-Medienmitteilung); Text nachgezogen | gebaut 0.1.37 |
| K100 | Notfall-QR «Noch nicht» weglassen? | Oktober |
| K101 | Hinweis «für alle lesbar» an allen drei QR | gebaut #231 |
| K106 | **Neu:** CSV-Budget-Import setzte Einkommen ohne Einkommenszeile auf 0 → beheben | gebaut 0.1.37 |
| UR | Quellen-Link auf das BVGer-Urteil C-409/2026 statt Medienmitteilung | gebaut 0.1.37 |
| — | Jakob-Vorschau, Studio-`noindex`, eingetragene Partnerschaft im Zivilstand, Vite-Major-Update | Oktober |

**Gebaut für 0.1.37-beta:** K82 · K86 · K87 · K88 · K89 · K90 · K94 · K95 · K96 · K97 (+ Flyer) · K98 (Texte) · K99 · K101 · K102 · K103 · K106. Gemeinsam getestet: 2008 Tests, 63.48/65 kB, PII 0.

**Neu offen**

| # | Punkt | Hand |
|---|---|---|
| K107 | CSV-Import: Zeilen mit Betrag 0 bei Miete/Nebenkosten/Krankenkasse werden ignoriert statt 0 geschrieben; Beträge ohne Kategorie landen in «other» und werden nie gespeichert | Entscheid (Oktober) |
| K108 | K82: Ausgaben ausserhalb des Formulars (MirrorCards `if (data.groceries)`) zeigen 0 nicht; Säule-3a-Summe 0 bleibt leer | Claude, klein |
| K109 | K90: im Fehlerfall ist die Sprachkarte erste Tab-Station vor dem Skip-Link | Browser prüfen |
| K110 | esbuild-Lücke (nur Dev-Server) schliesst erst ein Vite-Major-Update | Oktober |
| — | rm-Texte aus K86–K106 mit `TODO(rm)` → Fachperson (K69/K84) | Fachperson |
| K111 | Steuerrechner: die Gesamtbetrag-Box hat seit früher `aria-live: polite` und liest bei jeder Änderung vor → auf die neue kleine Live-Region umstellen? | a11y, Oktober |
| K112 | fr: Anrede-Umschalter fehlt (`main.jsx:842` nur de/it/rm), obwohl fr ~698 Sie/Du-Paare führt | Oktober |
| K113 | it: «stipendio» noch an 6 Stellen, «salario» ist Zielbegriff | Oktober |
| K114 | `NOTICE` nennt React (MIT) nicht unter den mitgelieferten Komponenten | Recht, Oktober |
| K115 | Bundle-Reserve knapp (63.48/65 kB) | beobachten |

**Deploy-Gate 0.1.37 (19.09.):** Sicherheit, Recht, Swiss Precision, Qualität, a11y, Wortlaut — je 0 🔴. Übernommen (Commit `1f46056`): Versicherergruppe am Ergebnis (Arztrechnung + Franchise-Tab), «Stand 2025» im Franchise-Tab, K99 «soll 2032» (Vorbehalte: Volksinitiative 29.11.2026, Gesetzesänderung), TI «amtl. Gesetzessammlung», Live-Ansagen Steuerrechner/QR, fr-Apostroph, TPW-Datenstand 19.09. Browser: Version, K103, K88-Linktexte, KK-QR-Hinweis, Konsole leer. **Nachtrag:** Herzensempfehlung **utopi** (Tiere, kein Affiliate) auf Wunsch von Stebler Studios mit in 0.1.37.

---

*Angelegt 15.09.2026. Nächste Fassung nach den Strichen von Stebler Studios. Verwandt:
`DURCHGANG-projekte-2026-09-15.md` · `website/LAUNCH-1-2-3.md` · Gedächtnis
`project_fokus_oktober_maloja_studio_2026-09-15`.*
