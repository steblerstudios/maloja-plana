# Maloja Plana — Phase 1 Full Implementation Tickets

> ⚡ = Critical Path Blocker | 🎯 = UX/Observability/Governance | 🔗 = High Context Dependency

---

## M1 — Runtime Foundation (Week 1–2)

| Task ID | Task Name | File Path / Component | Branch | Dependencies | Agent | Numbered Subtasks | Acceptance Criteria | Versioning / Persistence Notes | Context / Checkpoints | Thinking Framework |
|---------|-----------|----------------------|--------|--------------|-------|-------------------|---------------------|-------------------------------|----------------------|-------------------|
| ⚡ M1.1 | Event Bus | `src/runtime/events.js`, `src/runtime/index.js` | `feature/m1-event-bus` | — (none, first primitive) | Runtime Governance | 1. `mkdir src/runtime` 2. Implement `createEventBus()` returning `{ emit, on, off }` 3. Define typed event enum: `VALIDATION_PASS`, `VALIDATION_FAIL`, `STATE_TRANSITION`, `APPROVAL_REQUESTED`, `APPROVAL_GRANTED`, `APPROVAL_REJECTED`, `INGESTION_START`, `INGESTION_COMPLETE`, `AUDIT_ENTRY`, `MODULE_REGISTERED` 4. Implement wildcard `on('*', handler)` for cross-cutting concerns 5. Add `off(type, handler)` for cleanup on unmount 6. Unit tests: fire/receive, unsubscribe, wildcard receives all, no memory leaks after off() 7. Create barrel `src/runtime/index.js` exporting all | `npm test` passes; events fire synchronously; no DOM coupling; wildcard receives all; `off()` prevents leaks; zero dependencies added | No persistence — event bus is in-memory only. No IndexedDB, no localStorage. Bus instance lives in module scope, not window. | **CP-M1.1**: After step 2, verify `emit('X', data)` → listener receives `data`. After step 4, verify wildcard handler gets `{ type, payload }`. Debug: `console.log` in wildcard during dev, remove before merge. | Computational + Procedural: pure function design, no side effects outside listener callbacks, synchronous dispatch guarantees ordering |
| ⚡ M1.2 | State Machine | `src/runtime/stateMachine.js` | `feature/m1-state-machine` | M1.1 (emits STATE_TRANSITION) | Runtime Governance | 1. Implement `createStateMachine({ initial, states, eventBus })` 2. State config shape: `{ [stateName]: { on: { [eventName]: { target, guard? } } } }` 3. Add `guard: (context) => boolean` — blocks transition if false 4. On valid transition: emit `STATE_TRANSITION` via eventBus with `{ from, to, event, timestamp, context }` 5. Implement `getState()` returning current state string 6. Implement `canTransition(event)` returning boolean 7. Define field lifecycle: `empty→draft→validated→approved` 8. Define document lifecycle: `uploaded→validated→active→expired→archived` 9. Unit tests: valid transition, invalid rejection, guard blocking, event emission, lifecycle coverage | FSM rejects invalid transitions (returns false/throws); emits events on valid; guards block; `getState()` always reflects truth; no async | State machine is in-memory per instance. No persistence of FSM state itself — state is derived from data. Multiple independent instances allowed (one per document, one per field group). | **CP-M1.2**: After step 3, test guard `() => false` blocks transition. After step 4, verify event bus receives exact `{ from, to, event, timestamp }`. Debug: log transitions to console with `[FSM] ${from} → ${to} via ${event}`. | Logical + Computational: formal state machine theory, guard predicates as pure functions, transition tables as declarative config |
| ⚡🔗 M1.3 | Audit Logger | `src/runtime/auditLog.js` | `feature/m1-audit-log` | M1.1 (wildcard subscription) | Runtime Governance | 1. Open/create IndexedDB store `maloja-plana-audit` (version 1, object store `entries` with autoIncrement key + indexes on `type`, `timestamp`) 2. Implement `logEntry({ type, actor, payload, timestamp })` — append-only write 3. Implement `getEntries({ since?, until?, type?, limit? })` — newest-first, cursor-based 4. Implement `getEntryCount()` for dashboard display 5. Implement `initAuditLog(eventBus)` — subscribes wildcard, auto-logs all emissions 6. Define actor convention: `'user'` / `'system'` / `'agent:<name>'` 7. Unit tests with IndexedDB mock (fake-indexeddb): write, read, filter by type, filter by date range, count | Every event bus emission → audit entry within same tick; entries retrievable by type and date; count accurate; IndexedDB errors caught and logged (no crash) | **New IndexedDB store**: `maloja-plana-audit`, version 1. Object store: `entries` with `{ id (autoIncr), type, actor, payload, timestamp }`. Indexes: `by-type`, `by-timestamp`. Completely separate from existing `ordnung-ruhe-documents` and `ordnung-ruhe-backups`. | **CP-M1.3**: After step 1, verify DB opens without error in DevTools → Application → IndexedDB. After step 5, emit test event → verify entry appears in store. Debug: `indexedDB.open('maloja-plana-audit')` in console to inspect. Checkpoint: existing stores must remain untouched — verify `ordnung-ruhe-documents` still works after M1.3 merge. | Procedural + Analytical: append-only log design, IndexedDB transaction safety, cursor-based pagination for performance |
| ⚡ M1.4 | Module Registry | `src/runtime/registry.js` | `feature/m1-registry` | M1.1 (emits MODULE_REGISTERED) | Runtime Governance | 1. Implement `createRegistry(eventBus)` returning registry instance 2. `registerModule({ id, name, version, status })` — stores in Map, emits `MODULE_REGISTERED` 3. `getModules()` → array of all registered modules 4. `getModule(id)` → single module or null 5. `setModuleStatus(id, status)` — `'active'`/`'inactive'`/`'error'`, emits `STATE_TRANSITION` 6. Define built-in module IDs: `'validation'`, `'ingestion'`, `'approval'`, `'audit'` 7. Prevent duplicate registration (same id → update, not duplicate) 8. Unit tests: register, query, status transitions, duplicate handling, event emission | Modules self-register; status queryable via `getModules()`; event emitted per registration; duplicate-safe; no persistence (in-memory only) | In-memory Map only. Registry rebuilds on each app init (modules re-register). No IndexedDB/localStorage for registry state. Version field is for display only (human-readable semver string). | **CP-M1.4**: After step 2, register test module → verify `getModules()` returns it. After step 5, set status → verify audit log entry via M1.3. Debug: `registry.getModules()` in console. Verify built-in IDs match what M2–M5 will use. | Logical + Procedural: registry pattern, Map-based lookup, event-driven side effects |
| 🎯 M1.5 | Dashboard System Indicator | `src/Dashboard.jsx` (modify) | `feature/m1-dashboard-indicator` | M1.4 (reads registry) | UX Calmness (lead), Accessibility (review) | 1. Import registry from `src/runtime` 2. Compute aggregate: all active=sage, any error=rose, else gold 3. Render 8px dot in dashboard header (inline, no layout shift) 4. Add `aria-label={t('dashboard.systemHealthy')}` (or degraded/error variant) 5. Add i18n keys to en.js/de.js/fr.js/it.js: `dashboard.systemHealthy` ("System healthy"), `dashboard.systemDegraded` ("System degraded"), `dashboard.systemError` ("System error") 6. Verify dot visible in both light and dark palette 7. Verify no layout shift at 375px | Dot visible in header; correct color per aggregate status; accessible with aria-label; i18n in all 4 languages; dark mode contrast OK; no layout shift | No persistence — dot reads live from registry on each render. No caching of status. | **CP-M1.5**: Visual checkpoint — screenshot before/after at 1440px and 375px, light+dark. Verify dot doesn't push title. Test with registry in error state (mock one module error). | Analytical + Procedural: aggregate status computation, visual non-regression, accessible indicator design |

---

## M2 — Validation Engine (Week 2–3)

| Task ID | Task Name | File Path / Component | Branch | Dependencies | Agent | Numbered Subtasks | Acceptance Criteria | Versioning / Persistence Notes | Context / Checkpoints | Thinking Framework |
|---------|-----------|----------------------|--------|--------------|-------|-------------------|---------------------|-------------------------------|----------------------|-------------------|
| ⚡ M2.1 | Rule Schema | `src/runtime/validation/ruleSchema.js` | `feature/m2-rule-schema` | M1.1, M1.4 | Runtime Governance | 1. `mkdir src/runtime/validation` 2. Define rule shape: `{ id: string, field: string, type: RuleType, params: object, message: string, severity: Severity }` 3. Define RuleType enum: `required`, `minLength`, `maxLength`, `pattern`, `email`, `phone`, `date`, `range`, `custom` 4. Define Severity: `error`, `warning`, `info` 5. Implement `validateRuleDefinition(rule)` — returns `{ valid, errors[] }` 6. Create chapter rule sets: one exportable array per chapter key (persoenlich, wohnen, finanzen, versicherungen, ausbildung, behoerden, notfall) 7. Register `'validation'` module in registry on import 8. Unit tests: valid rule passes, invalid rule caught (missing id, unknown type, bad severity) | Rules are JSON-serializable plain objects; self-validation catches all malformed inputs; every chapter has at least 3 rules defined; module auto-registers | Rules are static code exports (not persisted). Rule definitions live in source code as const arrays. Future: rules could be loaded from file (M3 ingestion). No IndexedDB for rules themselves. | **CP-M2.1**: After step 5, pass deliberately malformed rule → verify structured error. After step 6, verify rule count per chapter matches existing `dataValidation.js` field checks. Cross-check: existing validation behavior must be reproducible from these rules. | Logical + Computational: schema validation theory, type system design, declarative rule definition, bijection with existing imperative checks |
| ⚡ M2.2 | Rule Evaluator | `src/runtime/validation/evaluator.js` | `feature/m2-evaluator` | M2.1, M1.1 | Runtime Governance | 1. Implement `evaluateRule(rule, value)` → `{ ruleId, passed: bool, value, message?, timestamp }` 2. Implement per-type evaluators: `evalRequired(v)`, `evalMinLength(v, params)`, `evalMaxLength(v, params)`, `evalPattern(v, params)`, `evalEmail(v)`, `evalPhone(v)`, `evalDate(v, params)`, `evalRange(v, params)`, `evalCustom(v, params)` 3. Implement `evaluateRuleSet(rules, data)` — batch: returns `[{ ruleId, passed, ... }]` 4. Emit `VALIDATION_PASS` / `VALIDATION_FAIL` per rule via event bus 5. Handle edge values: null→fail required, undefined→fail required, `''`→fail required, `'  '`→fail required 6. Unit tests: each type individually, batch mode, edge cases, event emission | Deterministic: same (rule, value) → same result always; events emitted; null/undefined/empty/whitespace handled; no async; no side effects beyond events | No persistence in evaluator itself. Results are ephemeral — evidence register (M2.3) handles storage. Evaluator is pure function + event emission. | **CP-M2.2**: After step 2, test each evaluator with known pass+fail inputs. After step 4, verify event bus receives correct type per evaluation. Regression: compare outputs against existing `validateField()` for identical inputs. Debug: `evaluateRuleSet(rules, testData)` in console with chapter data. | Computational + Logical: pure function evaluation, pattern matching, deterministic output guarantee, edge case enumeration |
| ⚡🔗 M2.3 | Evidence Register | `src/runtime/validation/evidenceRegister.js` | `feature/m2-evidence` | M2.2, M1.3 | Runtime Governance | 1. Implement `registerEvidence({ ruleId, field, result, inputSnapshot, timestamp })` — writes to `maloja-plana-audit` store with `type='validation'` 2. Implement `getEvidenceForField(field, { limit = 10 })` — query by field, newest first 3. Implement `getEvidenceForChapter(chapterKey)` — all evidence for fields in chapter 4. Implement `getValidationSummary(chapterKey)` → `{ total, passed, failed, warnings }` 5. Input snapshot: shallow copy of value at evaluation time (for provenance) 6. Unit tests: register, retrieve by field, retrieve by chapter, summary math | Every `evaluateRule` call that produces result → evidence stored; evidence retrievable per-field and per-chapter; summary counts accurate; input snapshots captured | Writes to existing `maloja-plana-audit` IndexedDB (created in M1.3), using type=`validation`. Evidence entries have same shape as audit entries but with extended payload containing `ruleId`, `field`, `inputSnapshot`. Shares store, filtered by type. | **CP-M2.3**: After step 1, trigger validation → verify entry in IndexedDB DevTools under `maloja-plana-audit` with type=`validation`. After step 4, validate summary math: `total = passed + failed + warnings`. Cross-check: M1.3 `getEntries({type:'validation'})` returns same data. | Analytical + Procedural: evidence-based design, append-only provenance, IndexedDB transaction reuse, aggregation queries |
| 🎯🔗 M2.4 | Migrate Existing Validation | `src/ChapterView.jsx`, `src/utils/dataValidation.js` | `feature/m2-migrate-validation` | M2.1, M2.2, M2.3 (full engine) | Runtime Governance + UX Calmness | 1. Map each `validateField(key, value)` case in `dataValidation.js` to equivalent rule in M2.1 schema 2. In `ChapterView.jsx`: import evaluator, replace `validateField` calls in `handleFieldBlur` with `evaluateRule(rule, value)` 3. After each evaluation, call `registerEvidence(...)` 4. Maintain exact same error display: red border + message text (no visual change) 5. Add badge to Dashboard: read `getValidationSummary(chapter)` → render `"3/12"` below chapter card 6. Add i18n: `dashboard.validationBadge` (en: "{passed}/{total} validated"), `dashboard.fieldsValidated` (en: "fields validated") 7. Full regression: test every field type that previously validated (email, phone, AHV, date, required) — must show identical errors | Zero UX regression (same red borders, same messages, same timing); evidence produced for every blur; badge visible on dashboard per chapter; i18n in all 4 languages | No new persistence — reuses M2.3 evidence register (writes to `maloja-plana-audit`). Existing `or5_` localStorage data schema unchanged. No migration needed. Dashboard badge reads from IndexedDB on mount. | **CP-M2.4**: CRITICAL REGRESSION CHECK. Before merge: test every validated field type in each chapter (email, phone, AHV-Nr, dates, required fields). Compare error messages character-by-character with pre-migration behavior. Screenshot before/after for each field type. Badge checkpoint: verify counts match manual count of validated fields. | Analytical + Procedural: migration safety, regression testing, visual non-regression, evidence-based refactoring proof |

---

## M3 — Source Ingestion (Week 3–5)

| Task ID | Task Name | File Path / Component | Branch | Dependencies | Agent | Numbered Subtasks | Acceptance Criteria | Versioning / Persistence Notes | Context / Checkpoints | Thinking Framework |
|---------|-----------|----------------------|--------|--------------|-------|-------------------|---------------------|-------------------------------|----------------------|-------------------|
| ⚡ M3.1 | File Parser | `src/runtime/ingestion/parser.js` | `feature/m3-parser` | M1.1 | Source Governance | 1. `mkdir src/runtime/ingestion` 2. Implement `readFile(file: File)` → `Promise<string>` via FileReader 3. Implement `parseJSON(content: string)` → `{ success, data, errors }` 4. Implement `parseCSV(content: string, { delimiter = ',', hasHeader = true })` → `{ success, data: object[], errors }` (header row → keys) 5. Implement `detectFormat(file: File)` → `'json'` / `'csv'` / `'unknown'` (extension + first-char sniff) 6. All functions return structured result — **never throw** 7. Error shape: `{ success: false, data: null, errors: [{ line?, column?, message }] }` 8. Unit tests: valid JSON, nested JSON, invalid JSON (parse error msg), CSV with headers, CSV without headers, CSV with semicolons, empty file, binary file (graceful fail) | Deterministic parsing; structured error objects (never throws); supports `,` and `;` delimiters; handles UTF-8 BOM; empty/binary files return structured error | No persistence — parser is pure transform. Input file is read once into memory string, parsed, result returned. Original file not stored (provenance hash computed in M3.3). | **CP-M3.1**: After step 3, test with malformed JSON → verify `errors[0].message` is human-readable. After step 4, test CSV with 100 rows → verify all parsed. Edge case: file with BOM character → verify handled. Debug: `parseCSV(content, {delimiter:';'})` for Swiss Excel exports. | Computational + Procedural: parser design, structured error handling, format detection heuristics, defensive programming |
| M3.2 | Schema Mapper | `src/runtime/ingestion/mapper.js` | `feature/m3-mapper` | M3.1, chapter field definitions | Source Governance | 1. Define mapping config: `{ [sourceField: string]: targetChapterField: string }` 2. Implement `createMapping(sourceFields: string[], targetChapter: string)` — auto-match by normalized field name (lowercase, strip spaces/underscores) 3. Implement `applyMapping(mapping, sourceData: object)` → chapter-format object 4. Implement `previewMapping(mapping, sourceData, currentData)` → `[{ field, oldValue, newValue, changed: bool }]` 5. Type coercion: string `"1500"` → number `1500` for numeric fields; `"2025-03-15"` → date string; `"true"`/`"false"` → boolean 6. Unmapped fields: include in preview as `{ field, unmapped: true }` for user awareness 7. Unit tests: auto-match, apply, preview diff, coercion, unmapped handling | Preview shows exact changes with old/new values; mapping configurable (user can override auto-match); type coercion handles Swiss formats; unmapped fields visible | No persistence — mapper is pure transform. Mapping config is ephemeral (generated per import session). Future: user could save mapping templates (not in Phase 1). | **CP-M3.2**: After step 2, test with source `["Vorname", "nachname", "E_Mail"]` → verify auto-matches to `persoenlich.firstName`, `persoenlich.lastName`, `persoenlich.email`. After step 4, verify diff shows only actually changed values. Debug: print unmapped fields to help user understand what wasn't imported. | Logical + Analytical: field name normalization, fuzzy matching, type inference, diff computation |
| ⚡🔗 M3.3 | Ingestion Pipeline | `src/runtime/ingestion/pipeline.js` | `feature/m3-pipeline` | M3.1, M3.2, M2.2, M1.1, M1.3 | Source Governance + Runtime Governance | 1. Define pipeline stages enum: `READ`, `PARSE`, `MAP`, `VALIDATE`, `PREVIEW`, `APPROVE`, `PERSIST`, `AUDIT` 2. Implement `createPipeline(file, chapterKey, mapping?)` → pipeline instance with `run()` method 3. Stage execution: each returns `{ stage, success, result?, error? }` — pipeline halts on first failure 4. Compute SHA-256: `crypto.subtle.digest('SHA-256', fileBuffer)` → hex string as provenance fingerprint 5. Before PERSIST stage: trigger `autoBackup.createBackup()` from existing `src/utils/autoBackup.js` 6. Emit `INGESTION_START` (with file metadata) and `INGESTION_COMPLETE` (with summary) via event bus 7. VALIDATE stage: run `evaluateRuleSet` from M2.2 against mapped data 8. APPROVE stage: returns Promise — resolved externally by approval gate (M4) 9. PERSIST stage: write to localStorage via existing `storage.set()` 10. Unit tests: full pipeline success, parse failure halts, validation failure halts, backup triggered | Pipeline stages observable via events; halts on any stage failure; SHA-256 provenance stored in audit; auto-backup fires before persist; approval gate integrated (Promise-based) | **Provenance**: SHA-256 of original file stored in audit entry payload. Persisted data goes to existing `or5_<chapter>` localStorage keys via `storage.set()`. Auto-backup snapshot created before write (existing `ordnung-ruhe-backups` IndexedDB). Pipeline progress NOT persisted (in-memory only, restartable). | **CP-M3.3**: HIGH CONTEXT DEPENDENCY. After step 4, verify SHA-256 matches known test file hash. After step 5, verify backup store has new entry (check IndexedDB `ordnung-ruhe-backups`). After step 9, verify `localStorage.getItem('or5_<chapter>')` contains new data. Critical: if pipeline fails mid-way, localStorage must be unchanged (no partial writes). Debug: log each stage transition. | Procedural + Analytical: pipeline architecture, transaction safety (no partial writes), cryptographic provenance, sequential stage coordination |
| 🎯🔗 M3.4 | Import UI | `src/ChapterView.jsx` (modify), `src/components/ImportPreview.jsx` (new) | `feature/m3-import-ui` | M3.3, M4.1 | UX Calmness + Accessibility | 1. Add "Import" button in ChapterView header: `React.createElement('button', { onClick: triggerFilePicker, 'aria-label': t('chapter.importFromFile') }, Icon + label)` 2. Hidden file input: `React.createElement('input', { type:'file', accept:'.json,.csv', ref: fileRef, onChange: handleFileSelect, style:{display:'none'} })` 3. `handleFileSelect`: create pipeline, run through PARSE+MAP stages, show ImportPreview 4. Create `ImportPreview.jsx`: bordered card showing `[{ field, oldValue, newValue }]` as rows 5. Row styling: changed=bold, unchanged=muted, new field=highlighted 6. Confirm button: `t('import.applyChanges')` — triggers APPROVE+PERSIST stages 7. Cancel button: `t('common.cancel')` — clears preview, no side effects 8. Success card: "✓ " + `t('import.success', { count: N })` + audit ref link 9. Error card: palette.rose border + `t('import.parseError')` + error details 10. i18n keys (all 4 locales): `chapter.importFromFile`, `import.preview`, `import.applyChanges`, `import.success`, `import.parseError`, `import.noChanges` 11. aria-labels: file input, confirm, cancel, preview region (`role="region"`) 12. Test 375px: card stacks, buttons full-width, preview scrollable | User can select file → see preview → confirm → data persisted → audit logged; OR cancel without side effects; OR see calm error on bad file; mobile-ready; accessible | No new persistence — uses existing `or5_` localStorage via pipeline (M3.3). Import preview state is component-local (useState). No draft saving. | **CP-M3.4**: Visual QA at 375px + 1440px, light + dark. Test with valid JSON → preview appears → confirm → verify localStorage updated. Test with invalid file → error card appears → verify no data change. Test cancel → verify localStorage unchanged. Accessibility: Tab through all controls, verify focus order. | Procedural + Analytical: multi-step UI flow, state management, error boundary design, accessible modal-like pattern without actual modal |

---

## M4 — Human Approval Gates (Week 5–6)

| Task ID | Task Name | File Path / Component | Branch | Dependencies | Agent | Numbered Subtasks | Acceptance Criteria | Versioning / Persistence Notes | Context / Checkpoints | Thinking Framework |
|---------|-----------|----------------------|--------|--------------|-------|-------------------|---------------------|-------------------------------|----------------------|-------------------|
| 🎯🔗 M4.1 | Gate Component | `src/components/ApprovalGate.jsx` (new) | `feature/m4-gate-component` | M1.1 (events) | UX Calmness (lead) + Accessibility (review) | 1. Create modal overlay: `position:fixed, inset:0, background: rgba(0,0,0,0.4), display:flex, align:center, justify:center, zIndex:1000` 2. Card: `palette.surface, padding:24px, borderRadius:12px, maxWidth:min(480px, 90vw), maxHeight:80vh, overflow:auto` 3. Props interface: `{ title: string, changes: [{ label, from?, to? }], onApprove: fn, onReject: fn(reason?) }` 4. Render title as h2 with `id` for aria-labelledby 5. Render changes as `<ul>` with `<li>` per change: `"label: from → to"` 6. Approve button: `palette.sage` bg, white text, `t('approval.approve')` 7. Reject button: `palette.border` bg, `palette.text`, `t('approval.reject')` 8. On reject click: show textarea for reason + "Confirm rejection" button 9. Escape key → `onReject(null)` (no reason) 10. Focus trap: on mount, focus first button; Tab cycles only within modal 11. Attributes: `role="dialog"`, `aria-modal="true"`, `aria-labelledby={titleId}` 12. i18n (4 locales): `approval.approve`, `approval.reject`, `approval.title`, `approval.reason`, `approval.confirmReject`, `approval.thisWill` 13. Dark mode: all colors from palette prop, zero hardcoded 14. 375px: buttons stack vertically (`flex-direction:column` below 400px) | Calm, non-urgent modal; no auto-dismiss/timeout; fully keyboard navigable; focus trapped; screen reader announces dialog; dark mode correct; mobile stacks buttons | No persistence — gate is ephemeral UI. Approval/rejection state communicated via callbacks only. Evidence logging happens in M4.3 (wiring layer), not in component itself. | **CP-M4.1**: Accessibility audit: open modal → verify focus on first button → Tab through all interactive elements → verify cycle. Escape → verify onReject called. Screen reader: verify "dialog" announced. Visual: screenshot at 375px dark mode with 3+ changes in list. Focus trap: verify Tab never escapes modal to background content. | Analytical + Procedural: modal accessibility patterns (WAI-ARIA dialog), focus management, responsive layout breakpoints, calm UX design principles |
| ⚡ M4.2 | Gate Registry | `src/runtime/gates/registry.js` | `feature/m4-gate-registry` | M1.4 (module registry) | Runtime Governance | 1. `mkdir src/runtime/gates` 2. Define `GATED_OPERATIONS` set: `'import'`, `'bulkEdit'`, `'delete'`, `'export'`, `'settingsChange'`, `'clearData'` 3. Define `EXEMPT_OPERATIONS` set: `'fieldEdit'`, `'navigation'`, `'viewChange'`, `'themeToggle'`, `'languageChange'` 4. Implement `requiresApproval(operationType: string)` → boolean (check GATED set) 5. Implement `requestApproval({ operation, changes, actor })` → `Promise<{ approved: bool, reason?: string }>` 6. Promise resolves only when gate component calls onApprove/onReject (no timeout, no auto-resolve) 7. Register `'approval'` module in registry 8. Unit tests: all gated ops return true, all exempt return false, unknown ops default to gated (safe default), Promise lifecycle | Correct classification of all operations; gate blocks indefinitely until human action; unknown ops default to requiring approval (fail-safe); module registered | No persistence — gate decisions are ephemeral. Evidence of decisions stored via M4.3 in audit log. Registry itself is in-memory. | **CP-M4.2**: After step 4, test each operation type explicitly. Verify unknown operation `'newThing'` → requires approval (fail-safe). After step 5, verify Promise stays pending until external resolution (no timeout). Integration: wire to M4.1 component in M4.3. | Logical + Procedural: fail-safe design (unknown = gated), Promise-based async flow, operation classification taxonomy |
| ⚡🔗 M4.3 | Approval Wiring + Evidence | `src/runtime/gates/evidence.js`, `src/DocumentTresor.jsx`, `src/ChapterView.jsx`, `src/ZipExport.jsx` | `feature/m4-wiring` | M4.1, M4.2, M1.3 | Runtime Governance | 1. Implement `logApproval({ operation, actor, timestamp })` → writes `APPROVAL_GRANTED` to audit log 2. Implement `logRejection({ operation, reason, actor, timestamp })` → writes `APPROVAL_REJECTED` to audit log 3. Create `useApprovalGate()` hook: `(operation, changes) => { showGate, gateProps, requestAndWait }` 4. Wire DocumentTresor delete: before `onDelete(doc.id)`, call `requestAndWait('delete', [{label: doc.fileName, from: 'exists', to: 'deleted'}])` 5. Wire import confirm (M3.4): APPROVE stage calls `requestAndWait('import', previewChanges)` 6. Wire ZipExport: before export generation, gate with `requestAndWait('export', [{label: 'Full backup export'}])` 7. If rejection: log rejection + return without executing action 8. Integration tests: delete→gate→approve→deleted→audit; delete→gate→reject→not deleted→rejection logged; import→gate→approve→persisted | All destructive operations show gate before execution; approval → action proceeds + evidence logged; rejection → action blocked + rejection logged with reason; hook reusable across components | Evidence stored in `maloja-plana-audit` IndexedDB (via M1.3 audit logger). Each approval/rejection creates one audit entry with full operation context. No separate persistence. | **CP-M4.3**: HIGH CONTEXT DEPENDENCY. Integration flow: click delete in Tresor → gate modal appears → approve → document actually deleted → verify audit entry exists with type `APPROVAL_GRANTED`. Repeat with reject → verify document still exists → verify `APPROVAL_REJECTED` entry with reason. Test hook reuse: same `useApprovalGate` works in Tresor, ChapterView import, ZipExport without modification. | Procedural + Analytical: hook composition, Promise coordination between UI and runtime, evidence chain integrity, integration testing across module boundaries |

---

## M5 — Audit & Observability (Week 6–7)

| Task ID | Task Name | File Path / Component | Branch | Dependencies | Agent | Numbered Subtasks | Acceptance Criteria | Versioning / Persistence Notes | Context / Checkpoints | Thinking Framework |
|---------|-----------|----------------------|--------|--------------|-------|-------------------|---------------------|-------------------------------|----------------------|-------------------|
| 🎯🔗 M5.1 | Audit Viewer | `src/components/AuditViewer.jsx` (new) | `feature/m5-audit-viewer` | M1.3, M1.1 | UX Calmness (lead) + Runtime Governance | 1. Create vertical timeline layout (`display:flex, flexDirection:column, gap:8px`) 2. Load entries via `getEntries({ limit: 50 })` on mount 3. Group by day: compute "Today"/"Yesterday"/formatted date headers 4. Entry card: bordered card with type badge (colored dot + label) + monospace timestamp + description text 5. Badge colors from palette: validation=`palette.sky`, approval=`palette.sage`, ingestion=`palette.gold`, system=`palette.mid` 6. Filter dropdown: `<select>` with options "All", "Validation", "Approval", "Ingestion", "System" — re-queries on change 7. "Load more" button at bottom: loads next 50 entries (offset-based) 8. Empty state: calm bordered card with `t('audit.noEntries')` + hint text 9. i18n (4 locales): `audit.title`, `audit.noEntries`, `audit.noEntriesHint`, `audit.loadMore`, `audit.filterAll`, `audit.filterValidation`, `audit.filterApproval`, `audit.filterIngestion`, `audit.filterSystem`, `audit.today`, `audit.yesterday` 10. Landmark: `role="log"`, `aria-label={t('audit.title')}`, `aria-live="polite"` for new entries 11. Test 375px: cards full-width, filter dropdown full-width, timestamps don't overflow 12. Test dark mode: all badge colors + card borders correct | Full audit history viewable; filterable by type; paginated (no infinite load); accessible (role=log); calm empty state; mobile-ready; dark mode correct | Reads from `maloja-plana-audit` IndexedDB via M1.3 `getEntries()`. No write operations. Viewer is read-only. Filter/pagination state is component-local (useState). | **CP-M5.1**: After step 3, verify day grouping with entries from today + yesterday + older. After step 6, filter to "Approval" → verify only approval entries shown. After step 7, verify "Load more" fetches next batch without duplicates. Performance: verify 200 entries render without jank. Dark mode: screenshot all badge colors. | Analytical + Procedural: timeline UI patterns, IndexedDB read optimization, pagination without duplication, accessible live region updates |
| 🎯 M5.2 | System Status Panel | `src/components/SystemStatus.jsx` (new), `src/Dashboard.jsx` (modify) | `feature/m5-system-status` | M1.4, M1.3 | UX Calmness + Runtime Governance | 1. Create `SystemStatus.jsx`: grid of module cards (`display:grid, gridTemplateColumns: repeat(auto-fit, minmax(140px, 1fr)), gap:8px`) 2. Each card: module name + status dot (8px, same colors as M1.5) + last event timestamp (relative: "2m ago") 3. Query `getModules()` from registry for list + status 4. Query `getEntries({ type: moduleType, limit: 1 })` per module for last event time 5. Section header: `t('system.title')` 6. i18n (4 locales): `system.title`, `system.active`, `system.inactive`, `system.error`, `system.lastEvent`, `system.noEvents` 7. Collapsible on viewports < 768px: starts collapsed, toggle button `t('system.showStatus')`/`t('system.hideStatus')` 8. Add to Dashboard.jsx below existing chapter grid 9. Dark mode: verify all dots + cards use palette | Module health at a glance; last activity time per module; non-intrusive on mobile (collapsed); palette colors only; accessible toggle | Reads from registry (in-memory) + audit log (IndexedDB). No write operations. Collapse state is component-local (useState), not persisted. | **CP-M5.2**: After step 3, verify all 4 built-in modules shown (validation, ingestion, approval, audit). After step 4, trigger a validation → verify "last event" updates. Mobile: verify collapsed by default at 375px, expand/collapse works. Dark mode screenshot. | Analytical + Procedural: dashboard composition, real-time status aggregation, responsive collapse pattern, relative time formatting |
| M5.3 | Audit Export | `src/components/AuditViewer.jsx` (modify) | `feature/m5-audit-export` | M5.1, M4.2 | Runtime Governance | 1. Add "Export" button below filter: `t('audit.export')` with download icon 2. On click: call `requestApproval('export', [{label: 'Audit log export'}])` from M4.2 3. On approve: `getEntries({ limit: 10000 })` → serialize as JSON array 4. Create Blob: `new Blob([json], {type:'application/json'})` 5. Trigger download: create `<a>` element with `URL.createObjectURL(blob)`, filename `maloja-plana-audit-YYYY-MM-DD.json` 6. After download: log `AUDIT_ENTRY` type `'audit_exported'` to audit trail 7. Strip any binary/blob references from entries before export (metadata only) 8. Add i18n: `audit.export` (en: "Export audit log") | Export button visible; triggers approval gate; generates valid JSON; downloads with correct filename; export event itself logged; no binary data in export | Exported JSON is a point-in-time snapshot. Not synced or versioned. Download goes to user's default download folder. Audit trail records that export happened (meta-audit). | **CP-M5.3**: Click export → gate appears → approve → verify .json file downloaded → verify file contains valid JSON array → verify no blob/binary data → verify new audit entry "audit_exported" in log. Test gate rejection → no download. | Procedural + Analytical: data export pipeline, approval gate integration, meta-audit (logging the log export), binary data exclusion |
| M5.4 | Retention Policy | `src/runtime/auditLog.js` (modify) | `feature/m5-retention` | M1.3 | Runtime Governance | 1. Add `pruneEntries(olderThanDays: number)` — opens cursor, deletes entries where `timestamp < (now - days)` 2. Add `RETENTION_DEFAULT = 90` constant 3. In `initAuditLog()`: call `pruneEntries(getRetentionSetting())` after DB open, before subscribing to events 4. After prune: log `{ type:'SYSTEM', actor:'system', payload: { action:'prune', deletedCount, olderThanDays } }` 5. Implement `getRetentionSetting()` — reads from localStorage `or5_settings.auditRetention` (default 90) 6. Add to settings UI (if exists) or NotificationSettings: dropdown with 30/60/90/180/365 days 7. i18n: `settings.auditRetention` (en: "Audit log retention"), option labels 8. Unit tests: prune removes old entries only, boundary (exactly 90 days = keep), empty store = no error, prune event logged | Store bounded; old entries pruned on startup; user controls via settings; prune itself logged (meta-event); default 90 days; boundary correct (day-exact) | Modifies `maloja-plana-audit` IndexedDB — deletes old entries. Retention setting stored in existing `or5_settings` localStorage object (no new keys pattern, extends existing settings). | **CP-M5.4**: Insert test entries with timestamps 30, 60, 90, 91, 120 days ago → prune with 90 → verify 91 and 120 deleted, 30/60/90 kept. Verify prune event in log after prune. Verify settings UI updates retention and next startup uses new value. Edge: empty store → no crash. | Computational + Procedural: time-based data lifecycle, IndexedDB cursor deletion, boundary-correct date math, self-referential logging |
| 🎯 M5.5 | System Navigation Tab | `src/App.jsx`, `src/MobileNav.jsx`, `src/SystemView.jsx` (new) | `feature/m5-system-nav` | M5.1, M5.2 | UX Calmness + Accessibility | 1. Add `'system'` to `VALID_VIEWS` set in `src/utils/hashRouter.js` 2. Create `src/SystemView.jsx`: renders AuditViewer + SystemStatus vertically with section spacing 3. In `App.jsx`: add route case `'system'` → render `<SystemView>` 4. In `MobileNav.jsx`: add nav item with system icon (`Icon name='system'` or ◎) + `t('nav.system')` 5. Position: after Calendar, before any settings link 6. i18n (4 locales): `nav.system` — en:"System", de:"System", fr:"Système", it:"Sistema" 7. Active state: same styling as other active nav items (border-bottom or bg highlight) 8. Test: click nav → URL changes to `#/system` → SystemView renders | New route `#/system` works; nav item visible and styled consistently; view renders both AuditViewer and SystemStatus; mobile nav correct | No new persistence — route is hash-based (existing pattern). View is composition only (no own state beyond child components). | **CP-M5.5**: Navigate to `#/system` directly in URL → verify view renders. Click nav item → verify navigation. Verify nav item active state matches other items. Mobile: verify nav item fits without overflow at 375px. Back button: verify returns to previous view. | Procedural: routing integration, component composition, navigation consistency, hash-based SPA pattern |

---

## M6 — Integration & Polish (Week 7–8)

| Task ID | Task Name | File Path / Component | Branch | Dependencies | Agent | Numbered Subtasks | Acceptance Criteria | Versioning / Persistence Notes | Context / Checkpoints | Thinking Framework |
|---------|-----------|----------------------|--------|--------------|-------|-------------------|---------------------|-------------------------------|----------------------|-------------------|
| ⚡🔗 M6.1 | E2E Integration Tests | `tests/e2e/` (new dir) | `feature/m6-e2e` | All M1–M5 | Release Safety (lead) | 1. **Import E2E**: create test JSON file → trigger file input → verify preview renders → approve gate → verify localStorage updated → verify audit entry with type `INGESTION_COMPLETE` + SHA-256 2. **Delete E2E**: render DocumentTresor with test doc → click delete → verify gate appears → approve → verify doc removed → verify `APPROVAL_GRANTED` in audit 3. **Validation E2E**: render ChapterView → type in field → blur → verify evaluator called → verify evidence in IndexedDB → verify badge count updated 4. **Rejection E2E**: trigger delete → gate appears → reject with reason "test reason" → verify doc still exists → verify `APPROVAL_REJECTED` with reason in audit 5. **Audit completeness**: after all flows, query audit log → verify every state transition has entry (no gaps) 6. **Offline E2E**: mock `navigator.onLine = false` → repeat import + delete flows → verify all work without network | All 4 critical flows pass; no silent failures; audit has zero gaps; offline works identically to online; all tests in CI (`npm test`) | Tests use in-memory IndexedDB mock (fake-indexeddb). Test fixtures stored as static JSON in `tests/fixtures/`. No real file system interaction. Tests are deterministic and fast (< 5s total). | **CP-M6.1**: FULL INTEGRATION CHECKPOINT. Run all E2E tests → all green. Then manually verify one full flow in browser DevTools: import file → check IndexedDB → check localStorage → check audit. Compare automated vs manual results. | Analytical + Procedural: integration testing strategy, fixture-based determinism, IndexedDB mocking, offline simulation |
| 🎯 M6.2 | Mobile QA (375px) | All new components | `feature/m6-mobile-qa` | All new UI (M1.5, M2.4, M3.4, M4.1, M5.1, M5.2, M5.5) | Accessibility + UX Calmness | 1. ImportPreview at 375px: verify card doesn't overflow, buttons are full-width, field diff scrollable 2. ApprovalGate at 375px: verify max-width 90vw applied, buttons stack vertically, reason textarea usable with keyboard 3. AuditViewer at 375px: verify entries don't overflow, filter dropdown full-width, timestamps readable (no cut-off) 4. SystemStatus at 375px: verify starts collapsed, toggle works, expanded cards stack single-column 5. System nav at 375px: verify item fits in MobileNav, no text overflow, active state visible 6. Dashboard badge at 375px: verify "3/12" badge doesn't break chapter card layout 7. Fix any horizontal scroll: check `document.documentElement.scrollWidth > window.innerWidth` 8. Fix any touch target issues: all buttons ≥ 44px tap area | Zero horizontal overflow at 375px; all buttons tappable (44px min); all text readable; collapse/expand works; no layout breaks | No persistence changes. QA-only branch — fixes CSS/layout issues found during testing. | **CP-M6.2**: Viewport at 375px in Chrome DevTools → navigate through every new view/component → no horizontal scrollbar at any point. Touch simulation: all buttons reachable. Screenshot each component at 375px for documentation. | Analytical + Procedural: responsive design verification, touch target compliance, viewport overflow detection, systematic component sweep |
| 🎯 M6.3 | Dark Mode QA | All new components | `feature/m6-dark-mode` | All new UI | UX Calmness | 1. Toggle to dark mode → verify ImportPreview: card bg=palette.up, border=palette.border, text=palette.text 2. ApprovalGate: backdrop semi-transparent, card=palette.surface, approve btn=palette.sage, reject=palette.border 3. AuditViewer: timeline bg=palette.surface, entry cards=palette.up, badge colors (sky/sage/gold/mid) all visible against dark bg 4. SystemStatus: dots visible (sage/mid/rose on dark), card borders distinct from background 5. Dashboard indicator dot: visible in dark header 6. Validation badge: text readable (palette.mid on palette.up) 7. Grep codebase for hardcoded colors in new files: `grep -rn '#[0-9a-fA-F]' src/components/Approval* src/components/Audit* src/components/System* src/components/Import*` → must return zero | Zero contrast issues; zero hardcoded hex colors in new component files; all visual elements use palette prop exclusively; WCAG AA contrast (4.5:1 for text) | No persistence changes. Fixes only — ensure all new components correctly reference `palette.*` properties. | **CP-M6.3**: Toggle dark mode → visit every new component → visual check. Run grep for hardcoded colors → must be zero. Optional: use axe DevTools for contrast ratio check on new components. Before/after screenshots in dark mode. | Analytical: systematic color audit, contrast ratio verification, palette consistency enforcement |
| M6.4 | Architecture Decision Records | `docs/architecture/adr-001..005.md` | `feature/m6-docs` | All modules implemented | Runtime Governance + Release Safety | 1. Write `adr-001-event-bus.md`: context (why sync, why in-memory, why wildcard), decision, consequences 2. Write `adr-002-validation-engine.md`: context (why declarative rules, why evidence), decision, consequences 3. Write `adr-003-ingestion-pipeline.md`: context (why staged, why SHA-256, why backup-before-write), decision, consequences 4. Write `adr-004-approval-gates.md`: context (why modal, why no timeout, why fail-safe), decision, consequences 5. Write `adr-005-audit-storage.md`: context (why separate IndexedDB, why append-only, why retention), decision, consequences 6. Update `docs/context/ARCHITECTURE_INDEX.md`: add links to all 5 ADRs 7. Update `PROJECT_STATUS.md`: reflect Phase 1 completion, new module list, updated build size | Every major decision documented with context, alternatives considered, and consequences; index updated; project status reflects current state | ADR files are documentation only — no code impact. Format: standard ADR template (Title, Status, Context, Decision, Consequences). | **CP-M6.4**: Each ADR must answer "why not the alternative?" For event bus: why not Redux/Zustand? For validation: why not Zod/Yup? For audit: why not localStorage? Peer review: another developer can understand each decision without prior context. | Logical + Analytical: architectural reasoning, alternative evaluation, consequence prediction, knowledge transfer |
| ⚡ M6.5 | Performance Verification | Build output, browser profiling | `feature/m6-performance` | All modules | Release Safety | 1. Run `npm run build` → verify gzip size < 200 KB (current baseline: 126 KB) 2. Lighthouse audit on built app: Performance score ≥ 90, FCP < 1.5s, LCP < 2.5s 3. Profile IndexedDB: run app for 5 min with typical usage (10 validations, 1 import, 2 approvals) → measure audit store size 4. Event bus leak check: mount/unmount components 100x → verify listener count returns to 0 via `eventBus._listeners` (or equivalent debug accessor) 5. Verify `package.json` dependencies section unchanged from pre-Phase-1 state 6. Verify no `node_modules` additions beyond devDependencies (test mocks allowed) | Build ≤ 200 KB gzip; Lighthouse ≥ 90; no memory leaks; zero new runtime deps; IndexedDB growth reasonable (< 1 MB for typical session) | Compare build output against baseline hash. Record metrics in `docs/architecture/performance-baseline.md` for future comparison. | **CP-M6.5**: RELEASE GATE. If build > 200 KB → investigate what grew → split or defer. If memory leak detected → fix before merge. Record: build size, Lighthouse scores, IndexedDB growth rate, listener count verification. This checkpoint blocks release. | Computational + Analytical: performance budgeting, memory profiling, dependency verification, regression detection |

---

## Summary

### Critical Path (⚡) — 12 tasks, sequential blockers:
```
M1.1 → M1.2 → M1.3 → M1.4 → M2.1 → M2.2 → M2.3 → M3.3 → M4.3 → M6.1 → M6.5
```

### Parallel Tracks (non-blocking):
```
Track A: M1.5 (after M1.4) — parallel to M2
Track B: M4.1 (after M1.1) — parallel to M2+M3
Track C: M3.1 → M3.2 (after M1.1) — parallel to M2
Track D: M5.1, M5.2, M5.4 (after M1.3) — parallel to M4
Track E: M6.2, M6.3, M6.4 — all parallel in Week 7–8
```

### High Context Dependency (🔗) — 7 tasks requiring cross-module awareness:
```
M1.3 (IndexedDB shared by M2.3, M5.1, M5.4)
M2.3 (reads/writes same store as M1.3)
M2.4 (must not regress existing UX)
M3.3 (coordinates M3.1, M3.2, M2.2, M1.3, autoBackup)
M3.4 (depends on M3.3 pipeline + M4.1 gate)
M4.1 (consumed by M3.4 import + M4.3 wiring)
M4.3 (wires gate into 3+ existing components)
M6.1 (tests all modules end-to-end)
```

### Thinking Framework Distribution:
```
Computational: M1.1, M1.2, M2.1, M2.2, M5.4, M6.5
Logical: M1.4, M2.1, M3.2, M4.2, M6.4
Analytical: M1.3, M1.5, M2.3, M2.4, M3.3, M4.3, M5.1, M5.2, M6.1, M6.2, M6.3
Procedural: M1.3, M3.1, M3.3, M3.4, M4.1, M5.3, M5.5, M6.2
```
